# 랩미팅 피드백 정리 및 개선 계획 — 2026-08-14

발표 자료: `PASN_lab_meeting_2026-08-14_spike_only_v2.pptx` (12 슬라이드)
작성: 2026-08-14 · 근거는 전부 저장소 내 기존 결과 파일에서 재계산

---

## 0. 판정 요약

| # | 피드백 | 판정 | 한 줄 |
|---|---|---|---|
| 1 | 실제 MBE 코드 직접 비교 (스파이크·지연·정확도·파라미터) | **부분적으로 이미 있음 + 발표가 안 밝힘** | op 층위는 진짜 MBE 런이다(45 빌드, 재현 검증). 네트워크 층위는 **0건**, 지연시간은 **저장소 전체에 0건** |
| 2 | 결과가 너무 긍정적 — 놓친 것·모순 찾기 | **타당. 헤드라인 5.27×는 범위 아티팩트** | 같은 JSON 안에 PASN 엄격 계상 2.871 mJ가 있고, 이건 MBE 스파이크-온리 1.413 mJ보다 **2.03× 나쁘다** |
| 3 | 메모리 증가량과 최적화 방안 | **타당. 절대량을 한 번도 안 보여줬다** | 전체 모델 37.3 KB → 52.6 KB (**+15.7 KB**). 둘 다 on-chip. 발표는 비율만 보여줘서 최악으로 읽혔다 |
| 4 | 곱셈·어텐션·LayerNorm은 메모리 증가 > 스파이크 절감 | **정확한 지적. 그리고 지목한 3개가 정확히 최악의 3개** | 10개 중 **5개**가 net loss. fp_multiply는 스파이크 1.23× 절감에 메모리 9.09× 증가 |

> **핵심**: 4개 피드백 중 3개(1·2·4)는 저장소 내부 문서 `PAPER_TABLE_COMPARISON.md` §2.4·§5·§6이
> **이미 🔴로 기록해 둔 것**이다. 데이터가 없어서 못 말한 게 아니라, **발표에 안 올렸다.**
> 이번 개선의 절반은 새 실험이 아니라 **이미 아는 약점을 발표 표면으로 올리는 것**이다.

---

## 1. 피드백 1 — 실제 MBE 뉴런 코드로 직접 비교

### 1.1 이미 있는 것 (그런데 발표에서 "MBE 코드를 돌렸다"고 말하지 않았다)

| 층위 | 상태 | 근거 |
|---|---|---|
| **뉴런 층위** | ✅ 있음 | `results/neuron_compare.json` — MBE N=4/N=8 vs PASN, 같은 함수에서 mse·spikes·params 동시 계측 |
| **op 층위** | ✅ 있음 | `results/op_pareto_merged.json` — `meta.backends = [mbe, pasn, pasn_rule_T, pasn_rule]`, `control_builds_checked: 45`, `control_reproduced: true`, **T ∈ {4, 8, 16} 각 50 레코드** |
| **네트워크 층위** | ❌ **0건** | 슬라이드 4·5·10의 MBE 값은 전부 **논문 published 표**(Table 3/4, Table XI) |
| **지연시간** | ❌ **저장소 전체 0건** | `latency|inference_time|throughput` grep → 전부 스윕 wall-clock 기록일 뿐 추론 지연 아님 |
| **파라미터** | ⚠️ 있으나 **미발표** | `results/e5_param_model.json` — 아래 참조 |

**즉 슬라이드 8·9(op Pareto)는 이미 code-vs-code 비교다.** 발표에서 이걸 명시하지 않아
교수님이 "전부 논문 인용 비교"로 읽으신 것으로 보인다. 이건 실험 부족이 아니라 **전달 실패**다.

### 1.2 파라미터 축 — 규약에 따라 결론이 뒤집히고, 논문 원문대로면 **진다**

`results/e5_param_model.json`:

| 규약 | MBE | PASN | 비율 | PASN 승? |
|---|---:|---:|---:|:--:|
| **G.1 원문대로** (항등원 파라미터 고정 = 학습 파라미터 없음) | 5,929 | 13,472 | **0.44×** | ❌ |
| 중립 (양쪽 다 사이트 간 공유 없음) | 16,611 | 30,023 | **0.55×** | ❌ |
| 항등원을 학습 뉴런으로 셈 (예전 읽기) | 16,611 | 13,472 | 1.23× | ✅ |

**3개 규약 중 2개에서 진다.** 그리고 이기는 1개는 논문 원문과 어긋나는 읽기다.

> G.1 인용: *"all learnable parameters in the MBE neuron are fixed"* (항등원에 대해)
>
> **논문을 문자 그대로 읽으면 PASN이 파라미터를 2.27× 더 쓴다.** 발표는 이 축을 아예 안 다뤘다.
> 교수님이 파라미터 수를 물으신 이상, 다음 발표에서 이 표를 **먼저** 보여줘야 한다.

보완 논거(이건 진짜 강점이라 같이 써야 함): 저장량이 **모델 폭과 무관**하다.
GPT-2(d=1024, 24층) 13,472 params · ViT-B(768, 12층) 6,500 · ViT-M(512, 12층) 6,404
→ `O(사이트 수)`, 사이트는 깊이 비례. **모델이 넓어질수록 상대 오버헤드가 줄어든다.**

### 1.3 🔴 2026-08-01 결정과의 정면 충돌 — 해소 필요

메모리 `pasn-positioning-decision`에 기록된 사용자 결정:

> **"이제부터 실험에서 MBE 뉴런 arm을 아예 돌리지 않는다.**
> 우리 재현이 완전하지 않으므로 기준선은 논문 표의 published 값만 쓴다."

**교수님 피드백 1은 이 결정을 정면으로 뒤집으라는 요구다.** 그냥 따르면 안 되고,
왜 그런 결정을 했는지와 함께 재검토해야 한다.

- 결정의 **원래 이유**: 우리 전역 MBE 재현이 **0 근처 GELU 해상도 바닥 ~0.07**을 못 넘어서
  downstream near-lossless 변환을 재현 못 함 → gpt2-medium GELU-only 변환 +254%.
  이 상태로 "PASN이 MBE를 이겼다"고 하면 **"전역 MBE가 나쁘다"와 "우리가 MBE를 못 맞춘다"를
  구분할 수 없다**(내부 문서에서 이 이유로 주장 C9를 삭제했다).
- **해소안**: MBE arm을 돌리되 **기준선을 두 개 병기**한다 —
  ① **우리-MBE**(재현, 한계 명시) ② **논문-MBE**(published 표).
  두 값의 격차 자체를 "우리 재현의 한계"로 정직하게 보고한다.
  이게 오히려 강하다. 지금은 격차를 **숨긴 채** 논문 표만 쓰고 있어서, 교수님 눈에는
  "왜 코드를 안 돌렸나"로 보인다.
- ⚠️ 그리고 이미 내부 문서 `PAPER_TABLE_COMPARISON.md` §6이 이 실험을
  **🔴 제출 전 반드시**로 올려놨다: *"저-T 우위가 전부 cross-paper이고 `mbe` 백엔드 저-T 런이 0건.
  이게 없으면 알려진 메커니즘 + 설명 안 되는 승리."*
  **교수님과 내부 판단이 독립적으로 같은 결론에 도달했다.** 우선순위 1번으로 확정.

### 1.4 🔴 그리고 우리 MBE op 런은 "T=8 붕괴"를 재현하지 않는다

`op_pareto_merged.json`에서 MBE 백엔드의 최저 nrmse (낮을수록 좋음):

| operator | T=4 | T=8 | T=16 |
|---|---:|---:|---:|
| activation:gelu | 0.025 | **0.005** | 0.005 |
| activation:exp2 | 0.008 | **0.003** | 0.002 |
| activation:inv | 0.007 | **0.003** | 0.002 |
| softmax | 0.044 | 0.016 | 0.005 |
| layernorm | 0.052 | 0.016 | 0.003 |
| **attention** | 0.247 | **0.081** | 0.016 |
| **fp_multiply** | 0.027 | **0.008** | 0.000 |

> **op 층위에서 MBE는 T=8에서 대체로 멀쩡하다.** gelu·exp2·inv는 T=16과 사실상 동일하다.
> 무너지는 건 **attention(5×)과 fp_multiply(20×)** 뿐이다.
>
> 그런데 슬라이드 5는 "MBE가 T=8에서 5개 모델 붕괴"라고 했다(논문 Table 4: ViT-M 1.17%).
> **논문의 네트워크 층위 붕괴를 우리 op 층위 MBE 런이 뒷받침하지 않는다.**
> 두 가지 중 하나다 — (a) 339개 사이트에 걸친 누적 효과, (b) 우리 MBE 재현이 논문과 다름.
> **이걸 가르는 게 E15의 핵심 목적이다.** 지금은 어느 쪽인지 모르면서 인과를 주장하고 있다.

또 하나: `pasn_rule` arm은 `T=None`이다(뱅크별 T_j를 **푸는** arm이므로 고정 T가 없다).
그래서 **op 층위에서는 저-T 직접 비교 자체가 아직 불가능**하다. T 예산을 8/4로 **상한**
건 `pasn_rule` arm이 필요하다.

---

## 2. 피드백 2 — 결과가 너무 긍정적이다 (가장 중요)

### 2.1 🔴 헤드라인 5.27×는 **범위 아티팩트**다

`results/energy_ratio_vitm.json` 한 파일 안에 두 숫자가 같이 있다:

| 양 | 값 | 무엇을 셌나 |
|---|---:|---|
| MBE 스파이크-온리 (as printed) | **1.413 mJ** | MBE 발화 × 0.9 pJ — **MBE의 전부** |
| **PASN 스파이크-온리** (발표에 쓴 값) | **0.268 mJ** | PASN 발화 × 0.9 pJ — **PASN의 일부** |
| **PASN 엄격 전체 계상** | **2.871 mJ** | + cmp · router · mac · poly · bitop |

- 발표의 **5.27× = 1.413 / 0.268** — MBE의 **전체**를 PASN의 **일부**로 나눈 값이다.
- 같은 파일의 엄격 계상을 쓰면 **2.871 / 1.413 = 2.03× PASN이 더 나쁘다.**

PASN 엄격 계상 내역 (ViT-M/16, per image):

| 클래스 | ops | mJ | 비중 |
|---|---:|---:|---:|
| **cmp** | 1,562.7M | **1.406** | 49.0% |
| ac | 595.9M | 0.536 | 18.7% |
| mac | 113.9M | 0.524 | 18.2% |
| **router** | 367.0M | **0.330** | 11.5% |
| poly | 16.2M | 0.015 | 0.5% |
| bitop | 61.2M | 0.000 | 0.0% |

> 🔴 **두 개의 치명적 사실:**
> 1. **PASN의 라우터 하나(0.330 mJ)가 PASN의 전체 스파이크 에너지(0.268 mJ)보다 크다.**
>    발표가 "빼고 센" 항목이 발표가 "센" 항목보다 크다.
> 2. **PASN의 cmp 하나(1.406 mJ)가 MBE의 전체 스파이크-온리(1.413 mJ)와 같다.**
>
> 슬라이드 11에 "router 비용 미포함"이라고 적긴 했다. 하지만 **포함하면 부호가 바뀐다는 말은
> 없다.** 한계로 적는 것과, 그 한계가 결론을 뒤집는다고 적는 것은 다르다.

⚠️ 공정을 위해: MBE 쪽 1.413 mJ도 MBE의 cmp/mac을 안 센 값이다. 그래서 2.03×도 최종 답이 아니다.
**양쪽을 같은 규칙으로 세는 게 E16**이고, 발표에서 이미 "다음 주"로 약속한 항목이다.
참고로 미커밋 상태인 `mbe_all_op()`를 돌려보면 MBE(비선형 op만) = **3.151 mJ**로 나온다.
PASN 엄격 2.871과 가까워서 **배수가 5.27×가 아니라 1.1× 근처**일 가능성이 크다.
(단 op 집합이 달라 그대로 나누면 안 된다 — §5 E16 참조.)

### 2.2 🔴 우위의 93.5%가 **모델링된 한 항**에서 나온다

MBE 스파이크 1,570.5M 중 **attention_score(FP 곱셈) 하나가 1,468.5M = 93.5%**.

이 항은 `E_FP = T²·η₁·η₂·MO` 공식에서 나오는데, 내부 문서 §2.4가 명시한다:

> *"구현으로 검증하지 않은 모델 공식"* — 논문은 두 피연산자를 어떻게 복원하는지 **명시하지 않는다.**

**즉 우리 헤드라인의 93.5%는 우리가 MBE에 대해 세운 가정 위에 서 있다.**
이 가정이 틀리면 5.27×는 통째로 무너진다. 발표는 이 의존성을 언급하지 않았다.

### 2.3 🔴 비선형 연산자만 보면 **우리가 진다**

| | MBE | PASN | 비율 |
|---|---:|---:|---:|
| FP 곱셈 (활성×활성) | 1,468.5M | 100.7M | **14.58×** |
| **비선형 op, as printed** | 102.0M | 197.2M | **0.52× 🔴 패 (1.9×)** |
| 비선형 op, per basis | 659.8M | 197.2M | 3.35× |

> **논문 공식을 문자 그대로 읽으면 비선형 연산자에서 PASN이 1.9× 진다.**
> 아이러니하게도 슬라이드 8·9는 비선형 연산자를 잔뜩 보여주면서 "10/10 승"이라고 했다.
> 그건 **등정확도 op 층위 매칭 비교**이고, 위 표는 **네트워크 층위 총량**이다. 둘 다 맞지만
> 발표는 유리한 쪽만 보여줬다.
>
> 내부 문서의 "쓰면 안 되는 문장" 목록에 이미 있다:
> ~~"비선형 연산을 더 싸게 한다"~~ — as printed 읽기에서는 1.9× 진다.

### 2.4 ⚠️ 슬라이드 6은 **자기 자신과의 비교**를 에너지 절감으로 제시했다

"T=8은 성능을 유지하며 spike-only E를 28–63% 줄였다" — 이건 **PASN@T=16 vs PASN@T=8**이다.
MBE와 무관하다. 그리고 spike-only E = 발화 × 0.9 pJ이므로, T를 절반으로 줄이면 발화가
대략 절반이 되는 건 **거의 항등식**이다. 에너지 절감 헤드라인 아래 놓일 결과가 아니다.

살릴 방법: 이 슬라이드의 진짜 메시지는 "T를 절반으로 줄여도 성능이 유지된다"(견고성)이지
"에너지를 줄였다"가 아니다. **헤드라인을 견고성으로 바꾸면 정직해지고 여전히 의미 있다.**

### 2.5 ⚠️ ANN 기준선이 비대칭이다

슬라이드 4는 "절대 점수 비교 금지"라고 정확히 명시했다. 그런데 헤드라인은
"세 모달리티에서 ANN 성능을 유지하거나 개선했다"이다.

| 모델 | MBE ANN | PASN ANN | 차이 |
|---|---:|---:|---|
| ViT-M | 85.950 | **84.922** | PASN이 **1.03 pp 낮은 데서 출발** |
| RoBERTa SST-2 | 94.490 | 94.037 | PASN이 0.45 pp 낮음 |
| GPT-2 WikiText-103 | 22.650 | 21.347 | PASN이 유리 |

> **더 낮은 ANN에서 출발하면 보존이 더 쉽다.** 3개 중 2개에서 PASN의 출발점이 낮다.
> 내부 문서도 인정한다: *"ImageNet은 우리 파이프라인이 0.8–1.0 pp 낮다(미규명)."*
> "변화량만 비교한다"는 규칙 자체는 맞지만, **출발점 차이를 슬라이드 본문에 안 적으면**
> 헤드라인이 그 이득을 조용히 가져간다.

### 2.6 ⚠️ 발표가 내부 문서의 금지 목록을 어겼다

`PAPER_TABLE_COMPARISON.md` §7 "쓰면 안 되는 문장":

| 금지 문장 | 발표에서 |
|---|---|
| "비선형 연산을 더 싸게 한다" | 슬라이드 8 헤드라인이 사실상 이 인상을 준다 (§2.3) |
| 총합을 반쪽 구분 없이 인용 | 슬라이드 10·12가 5.27×를 §2.4 단서 없이 인용 |
| "라우터는 공짜다" | 슬라이드 11이 "router 비용 미포함"이라 적었지만, JSON에는 **이미 11.5%로 계상돼 있다** |

> 마지막 항목은 방향이 반대인 실수다. 우리는 **라우터에 원소당 6 ops를 후하게 청구하고도
> 11.5%**라는 좋은 결과를 갖고 있는데, 발표는 "안 셌다"고 축소해서 말했다.
> 쓸 문장은 **"라우터에 원소당 6 연산을 청구하고도 11.5%"**다.

### 2.7 🔴 novelty가 이미 축소됐는데 발표는 반영 폭이 작다

`RELATED_WORK.md` §6b (2026-08-13 조사, 발표 하루 전):

- **Lee et al., TVLSI 2009 «Hierarchical Segmentation»** — 2의 거듭제곱(binade) 분할 +
  leading-zero 검출로 세그먼트 주소 계산 + **오차 허용치에서 세그먼트 수를 자동으로 푸는 것**까지 전부 선행.
- **NPE (arXiv:2104.06535)** — GELU 0 근처 진단을 **near-verbatim으로 선점**.
- **BBAL (arXiv:2504.15721)** — 지수 필드 라우팅 → per-range 서브테이블.
- → *"라우팅이 공짜다"* 차별점 **철회됨**. 살아남는 것은 **둘뿐**:
  ① 스파이크 도메인 자원(N_j, T_j) 배분 ② 저-T 실패 모드.

슬라이드 3이 "Routing 자체는 선행기술"이라고 적은 건 잘한 일이다. 하지만
**살아남은 두 기여 중 ②(저-T)가 §1.4에서 본 대로 아직 code-vs-code로 입증이 안 됐다.**
즉 지금 상태는 내부 문서 표현대로 *"알려진 메커니즘 + 설명 안 되는 승리"*에 가깝다.
**E15가 novelty의 절반을 지탱한다.**

### 2.8 기타 확인된 약점 (발표에 노트로만 있거나 없음)

| 항목 | 내용 |
|---|---|
| PASN도 무너진다 | T=2에서 ΔPPL **+60.97%**. 슬라이드 5 노트에만 있고 본문엔 없음 |
| fp_multiply 증거가 가장 약함 | matched **2/3**뿐인데, 이 op가 §2.2에서 본 대로 우위의 93.5%를 지탱 |
| E14는 부분 ablation | identity router 고정. 슬라이드 11에 명시됨 ✅ |
| 스케일 | 최대 345M. 경쟁 연구(NLSpike)는 8B급 4종 |
| E7 결과가 발표에 아예 없음 | 메모리 축을 통째로 뺐다 → 피드백 3·4를 자초함 |
| op 층위 byte 비율과 전체 모델 byte 비율이 불일치 | 1.08–9.50× vs 1.41× — 조건이 다름 (§3.1) |

---

## 3. 피드백 3 — 메모리가 얼마나 늘고, 최적화 방안이 있는가

### 3.1 절대량을 한 번도 안 보여준 게 문제였다

`results/e7_memory_cost.json` (GPT-2-medium, 339 사이트):

| | 저장량 | 파라미터 |
|---|---:|---:|
| 전역 MBE (de-dup) | **37.3 KB** | 5,929 ~ 16,611 (규약별) |
| **PASN** | **52.6 KB** | 13,472 |
| **차이** | **+15.7 KB (+41%)** | |

- 사이트당 뱅크 테이블: **중앙값·최대 모두 1 KB 미만**. 339개 전부 합쳐도 **64 KB SRAM 하나에 들어간다.**
- → **양쪽 다 on-chip. DRAM(640 pJ/access)은 어느 쪽 경로에도 없다.**

> **발표는 op 층위 비율(1.08–9.50× 증가)만 보여줬다.** 그래서 "9.5배 증가"로 들렸고,
> 교수님이 "MoE 단점이라도 trade-off가 납득 가능해야 한다"고 하신 게 당연하다.
> **절대량으로 말하면 +15.7 KB다.** 이 숫자 하나가 피드백 3의 절반을 해소한다.

⚠️ 단, 정직하게: 위 두 값은 **동일 조건이 아니다.** MBE 37.3 KB는 논문 G.1 설정(N=4/8)이고,
op 층위의 1.08–9.50×는 **등정확도 매칭 점**이다. 우리 MBE는 0 근처 GELU 바닥 때문에
PASN 정확도에 애초에 도달하지 못하므로, **진짜 등정확도 MBE는 더 많은 바이트를 쓸 것이다.**
이 불일치를 정리하는 게 E18의 일부다.

### 3.2 🔴 그런데 dataflow가 결론을 뒤집는다 — 이건 발표에 없었다

E7은 두 dataflow를 bracket으로 잡는다 (Horowitz ISSCC 2014, 45 nm):

| dataflow | access | PASN pJ/elem | MBE pJ/elem | 비율 | PASN 메모리/연산 |
|---|---|---:|---:|---:|---:|
| element-stationary | register | 1.15 | **0** | — | **1.8%** |
| element-stationary | SRAM 64KB | 230.7 | **0** | — | **26.9%** 🔴 |
| tensor-stationary | register | 8.68 | 36.39 | **4.19×** | 13.4% |
| tensor-stationary | SRAM 64KB | 1736.8 | 7278.8 | **4.19×** | 26.9% |

- **element-stationary에서 우리는 구조적으로 진다.** 전역 뉴런의 파라미터는 loop-invariant라
  레지스터로 hoist되어 **한계비용 0**이다. 0을 이길 수는 없다.
- 그래서 그 열에서 읽어야 할 건 비율이 아니라 **"연산 대비 몇 %를 더 쓰나"**다.
  레지스터 급이면 **1.8%**(사이트당 뱅크 세트가 1 KB 미만이므로 이쪽이 타당),
  풀 SRAM 접근이면 **26.9%로 에너지 스토리가 살아남지 못한다.**
- **tensor-stationary에서는 4.19× 이긴다** — 스파이크를 아끼는 것과 같은 메커니즘
  (state traffic이 **active** basis 수에 비례: PASN 1.57 vs 전역 7.58).

> **정직한 답변: "dataflow에 따라 다르고, 우리가 고를 수 없다. 양쪽 다 보고한다."**
> 이게 발표에 없었기 때문에 피드백 3이 나왔다.

### 3.3 최적화 레버 — 이미 측정된 것 3개

| 레버 | 효과 | 근거 |
|---|---|---|
| **공유 basis (MBE-PASN-S)** | 메모리 **1.6–3.7× 감소**, 정확도는 **오히려 향상**(GELU 5.2×, SiLU 6.1×, 1/√x 21×) | `shared_banks_report.md`. **단 최소 스파이크 점은 flat이 보유** — S는 스파이크를 못 이긴다 |
| **파라미터 공유** | 30,023 → **13,472** (2.23× 감소) | `e5_param_model.json`의 `pasn_params_no_sharing` |
| **라우터 산술 축소** | 11.5% → **약 4%** | 뱅크 입력 affine은 IEEE 지수 덮어쓰기(bitop), clamp는 saturating 인덱스로 접힘. 6 ops → 2 ops. ⚠️ **모델링이지 측정 아님** — sensitivity로만 |

**미탐색 레버** (E18/E19에서 다룰 것): 뱅크 계수 저정밀 양자화(fp32 → int8/fp16),
사이트 간 뱅크 테이블 공유, net loss op에서 라우팅 끄기(§4.3).

---

## 4. 피드백 4 — 스파이크 절감보다 메모리 증가가 크다

### 4.1 지적이 정확하다. 그리고 지목하신 3개가 정확히 최악의 3개다

`results/op_pareto_merged_wins.md`에서 재계산 (등정확도 매칭 중앙값):

| operator | 스파이크 절감 | 메모리 증가 | **net** | 판정 | matched |
|---|---:|---:|---:|:--:|:--:|
| **fp_multiply** | 1.23× | **9.09×** | **0.14** | 🔴 LOSS | 2/3 |
| **layernorm** | 1.55× | **4.55×** | **0.34** | 🔴 LOSS | 6/8 |
| **attention** | 1.70× | **3.12×** | **0.54** | 🔴 LOSS | 6/6 |
| activation:tanh | 3.91× | 5.26× | 0.74 | 🔴 LOSS | 8/8 |
| activation:silu | 2.63× | 3.33× | 0.79 | 🔴 LOSS | 7/7 |
| softmax | 4.23× | 3.85× | 1.10 | ~tie | 7/7 |
| activation:gelu | 4.95× | 2.86× | 1.73 | ✅ win | 7/7 |
| activation:inv | 5.39× | 2.56× | 2.10 | ✅ win | 8/9 |
| activation:invsqrt | 2.27× | 1.08× | 2.11 | ✅ win | 6/10 |
| activation:exp2 | 4.48× | 1.96× | 2.28 | ✅ win | 9/10 |

> **10개 중 5개가 net loss.** 교수님이 지목하신 곱셈·어텐션·LayerNorm이 **최악 1·2·3위**다.
> 지적을 방어할 게 아니라, **이 표를 우리가 먼저 만들어서 보여줬어야 했다.**

### 4.2 다만 이 비율 대 비율 비교는 **차원이 맞지 않는다** — 이게 우리의 실질 반론

- **스파이크**는 추론마다 반복되는 **동적** 비용이다.
- **바이트**는 한 번 올려놓는 **정적** 저장이다.
- 둘을 1:1 배수로 놓으면, 추론을 100만 번 하든 1번 하든 같은 결론이 나온다. 그건 틀렸다.

**정적 저장의 추론당 비용은 "바이트 수"가 아니라 "접근 에너지"다.** 그리고 그건 E7이 이미
가격을 매겼다 — §3.2의 **연산 대비 1.8%(레지스터) ~ 26.9%(풀 SRAM)**.

수치로 보면:
- fp_multiply의 "9.09× 메모리"는 절대량으로 **24 B → 252 B, 즉 +228 B**다.
- attention의 "3.12×"는 **약 128 B → 1,120 B**다. 사이트당 **1 KB 남짓**.
- 전체 모델로 합치면 §3.1의 **+15.7 KB**이고, 이건 on-chip에 들어간다.

> **그러므로 옳은 질문은 "메모리가 몇 배 늘었나"가 아니라
> "늘어난 메모리의 추론당 접근 에너지가 절감한 스파이크 에너지보다 큰가"다.**
> 지금 우리는 이 질문을 **전체 모델 수준에서만** 답했고(E7), **op별로는 답한 적이 없다.**
> 교수님 질문은 정확히 op별 답을 요구한다. → **E18**

### 4.3 🔴 그런데 정직하게: op별로 계산하면 일부는 **진짜로 질 것**이다

fp_multiply(1.23×)·layernorm(1.55×)·attention(1.70×)은 스파이크 절감 폭 자체가 작다.
여기에 라우터 6 ops/element + 뱅크 접근을 청구하면 **net이 음수가 될 가능성이 높다.**
(참고: 네트워크 전체에서 라우터 0.330 mJ > PASN 전체 스파이크 0.268 mJ — §2.1)

**그러면 결론은 "PASN을 쓰지 말자"가 아니라 "어디에 쓸지를 규칙으로 풀자"다:**

> **Hybrid 배분 — net 이득이 양수인 op에만 라우팅을 켜고, 나머지는 전역 MBE를 쓴다.**

이게 방법론적으로 자연스러운 이유: PASN의 중심 주장이 이미
*"용량을 손으로 고정하지 말고 오차 제약에서 **푼다**"*이다.
**"어느 op에 라우팅을 쓸지"도 같은 규칙으로 푸는 것**이 일관된 확장이고,
"왜 쓰는지 설득력이 없다"는 지적에 대한 **정면 답변**이다.
예상 결과: 활성함수(gelu·exp2·inv·invsqrt)는 켜고, fp_multiply·layernorm·attention은
끄거나 공유-basis(§3.3)로 전환.

⚠️ 리스크: attention은 ViT 예산의 68–70%, GPT-2의 81.8%다. 여기서 라우팅을 끄면
**전체 에너지 우위가 대부분 사라진다.** 그래서 E18은 "최적화"가 아니라 **방법의 적용 범위를
정하는 판정 실험**이다. 결과가 나쁘면 논문의 주장 범위를 좁혀야 한다.

---

## 5. 개선 계획

### 우선순위

| # | 실험 | 대응 피드백 | 비용 | 왜 이 순서인가 |
|---|---|---|---|---|
| **E15** | MBE 백엔드 네트워크 층위 실행 + 4축 동시 계측 | **1** | ~5–8h GPU | novelty의 절반(저-T)이 여기 달렸고, 교수님·내부 문서가 독립적으로 같은 결론 |
| **E16** | 대칭 All-op 에너지 계상 | **2** | 로컬 1일 | 발표에서 이미 약속함. 5.27×의 운명이 여기서 결정 |
| **E18** | op별 메모리 접근 에너지 + hybrid 라우팅 게이트 | **3·4** | 로컬 2일 | 피드백 3·4 정면. 방법의 적용 범위를 정함 |
| **E17** | 추론 지연시간 최초 측정 | **1** | 로컬 1일 | 저장소에 0건. 단 대리변수 한계가 커서 3순위 |
| **E19** | 공유-basis로 loss op 메모리 회수 | **3·4** | 로컬 1–2일 | E18 결과에 의존 |
| **E20** | 파라미터 축 3규약 정직 보고 | **1** | 로컬 반나절 | 이미 데이터 있음. 표 만들기 |

---

### E15 🔴 — MBE 백엔드 네트워크 층위 실행 (최우선)

**목적**: 슬라이드 4·5·10의 cross-paper 비교를 code-vs-code로 교체.
그리고 §1.4의 미해결 질문 — MBE의 T=8 붕괴가 **누적 효과인가, 우리 재현 문제인가** — 판정.

**방법**
- `src/mbe/convert.py`의 `mbe` 백엔드로 ViT-M/16 + GPT-2-medium 변환.
- 설정은 논문 G.1 그대로: GELU/Tanh **N=4**, 항등원·2^x·1/x·1/√x **N=8**, lr 0.01, 200 epochs, decay 0.99.
- **T ∈ {16, 8, 4}**, PASN arm과 **같은 체크포인트·같은 평가 레시피**.
- 4축 동시 계측: **spikes/input · accuracy(PPL) · params(3규약) · latency(E17)**.

**산출**: `results/e15_mbe_arm.json` + 4축 비교표 1장

**판정 기준 (미리 정해둘 것 — 결과 보고 정하면 안 됨)**
| 결과 | 해석 | 발표에서 할 말 |
|---|---|---|
| 우리-MBE도 T=8에서 무너짐 | ✅ 슬라이드 5 인과 성립 | "우리 코드로도 재현했다" — 가장 강한 결과 |
| 우리-MBE가 T=8에서 버팀 | 🔴 논문 Table 4와 우리 재현이 갈림 | **저-T 주장을 축소하고**, 격차를 "우리 재현 한계"로 보고 |

**필수 조건**: 기준선을 **두 개 병기** — ①우리-MBE(0 근처 GELU 바닥 ~0.07 명시)
②논문-MBE(published). 격차를 숨기지 말 것 (§1.3).

---

### E16 🔴 — 대칭 All-op 에너지

**목적**: 5.27×가 살아남는지 판정. **발표에서 이미 다음 주로 약속한 항목.**

**현황**: `experiments/energy_ratio_vitm.py`에 `mbe_all_op()`가 **미커밋 상태로 이미 구현돼 있다.**
돌리면 MBE(비선형 op만) = **3.151 mJ** vs PASN 엄격 = **2.871 mJ**.

**막고 있는 것 하나**: MBE의 **FP 곱셈 피연산자 복원 구조를 논문이 명시하지 않아**
MBE의 compare 수를 유도할 수 없다. 그런데 그 경로가 우리 우위의 93.5%다.

**해결책 — 배수 하나가 아니라 bracket 두 개로 보고한다:**
| 가정 | MBE FP곱셈 처리 | 결과 |
|---|---|---|
| A (관대) | `T²·η₁·η₂·MO` 모델 유지 | 우리에게 유리한 상한 |
| B (보수) | 비선형 op만, 양쪽 다 FP곱셈 제외 | 우리에게 불리한 하한 |

**산출**: 같은 op 집합 · 같은 45nm 단가 · 같은 입력 단위 표 1장 + **배수의 구간**
**할 일**: `mbe_all_op()` 커밋, per-basis / as-printed 읽기 명시, 두 bracket 계산.
**예상**: 배수가 5.27× → **1.1–2× 구간**으로 크게 축소될 것. 이걸 미리 각오하고 발표할 것.

---

### E18 🔴 — op별 메모리 접근 에너지 + hybrid 라우팅 게이트 (피드백 3·4 정면)

**목적**: "메모리가 스파이크 절감보다 많이 는다"에 **에너지 단위로** 답하고,
**어느 op에 PASN을 쓸지를 규칙으로 푼다.**

**방법**
1. E7(현재 전체 모델)을 **op별로 내린다**: op마다 파라미터 읽기 + state traffic을
   두 dataflow × 세 access 급으로 가격.
2. op별 **net = (절감한 스파이크 에너지) − (추가된 라우터 + 접근 에너지)**.
3. **게이트 규칙**: net ≤ 0인 op는 라우팅 끄고 전역 MBE 사용 → hybrid 구성.
4. hybrid 전체 모델 에너지를 E16의 대칭 계상으로 재산출.
5. §3.1의 불일치 정리: **등정확도 조건에서** MBE 바이트를 다시 재서
   전체 모델 1.41×와 op별 1.08–9.50×를 같은 조건으로 통일.

**산출**: `results/e18_op_net_energy.json` + "라우팅을 쓸 op / 안 쓸 op" 규칙 표

**판정 기준**
| 결과 | 대응 |
|---|---|
| 활성함수만 net > 0 | hybrid로 재구성. **논문 주장 범위를 "비선형 활성함수"로 좁힌다** |
| attention도 net > 0 | 현재 주장 유지 가능 (최선의 경우) |
| 대부분 net < 0 | 🔴 방법의 에너지 주장을 철회하고 **정확도/저-T 견고성**으로 축을 옮긴다 |

⚠️ attention이 ViT 예산의 68–70%, GPT-2의 81.8%이므로 **이 실험이 전체 스토리를 좌우한다.**

---

### E17 🟡 — 추론 지연시간 최초 측정

**목적**: 저장소에 **0건**인 축을 채운다.

**⚠️ 가장 조심해야 할 실험이다.** PyTorch CPU 시뮬레이션의 wall-clock은
**하드웨어 지연의 대리변수가 아니다.** SNN을 GPU에서 시뮬레이션한 시간은 구현 품질을
잴 뿐이다. 그래서 **두 가지를 분리해서** 보고한다:

1. **(a) 시뮬레이션 wall-clock** — MBE arm vs PASN arm, 동일 하드웨어·배치·seed.
   "우리 구현이 얼마나 느린가"만 말한다. 방법의 우열 근거로 쓰지 않는다.
2. **(b) 임계경로 분석** — 라우터 지연(지수 추출 → 뱅크 인덱스 → 뱅크 로드)을
   원소당 게이트 단계로 계상. 참고: Lee et al. TVLSI 2009가 **leading-zero 검출로
   주소 로직 지연을 최소화하는 것을 명시적 동기로 삼았다**(`RELATED_WORK.md` §6b) —
   즉 이 지연은 선행연구가 이미 다룬 알려진 항목이고, 그 해법도 알려져 있다.

**산출**: `results/e17_latency.json` + "우리가 아는 것 / 모르는 것" 명시한 표

---

### E19 🟡 — 공유-basis로 loss op 메모리 회수 (E18 의존)

E18에서 net < 0으로 판정된 op(fp_multiply·layernorm·attention 유력)에
**MBE-PASN-S**(공유 basis + 라우팅된 readout)를 적용.
근거: 메모리 **1.6–3.7× 감소**하면서 정확도는 오히려 향상.
⚠️ 단 **S는 스파이크를 못 이긴다**(flat이 최소 스파이크 점 보유) — 메모리 회수와
스파이크 손실의 교환비를 E18의 net 지표로 판정.

추가 탐색: 뱅크 계수 **저정밀 양자화**(fp32 → fp16/int8). 저장을 2–4× 줄이는
가장 단순한 레버인데 아직 안 해봤다.

---

### E20 🟡 — 파라미터 축 정직 보고

`e5_param_model.json`의 3규약 표를 그대로 슬라이드화 (§1.2).
**G.1 원문대로면 0.44×로 진다는 것을 먼저 적고**, 이어서
"저장이 모델 폭과 무관하다(`O(사이트 수)`)"는 보완 논거를 붙인다.
데이터는 이미 있고, 표만 만들면 된다.

---

## 6. 다음 발표 재구성안

현재 12슬라이드는 **긍정 결과 8 : 한계 1** 구조다. 아래로 바꾼다.

| 슬라이드 | 내용 | 변경 |
|---|---|---|
| 1 | 지난 피드백 4건과 대응 | 🆕 **피드백으로 시작한다** |
| 2 | **5.27×의 해부** — 무엇을 셌고 무엇을 뺐나 | 🔴 **가장 먼저 정정.** 라우터 0.330 > 스파이크 0.268 |
| 3 | E16 대칭 계상 결과 (bracket 두 개) | 🆕 |
| 4 | E15 MBE code-vs-code 4축 표 | 🆕 **"MBE 코드를 돌렸다"를 명시** |
| 5 | 파라미터 3규약 — **지는 규약을 먼저** | 🆕 (E20) |
| 6 | 메모리 **절대량** 37.3 → 52.6 KB, 둘 다 on-chip | 🆕 (§3.1) |
| 7 | dataflow bracket — 이기는 쪽과 지는 쪽 | 🆕 (§3.2) |
| 8 | op별 net 에너지 표 (5/10 loss를 **우리가 먼저**) | 🆕 (§4.1) |
| 9 | **hybrid 라우팅 게이트** — "어디에 쓸지도 푼다" | 🆕 (§4.3) — 피드백 4에 대한 답 |
| 10 | 저-T 견고성 (T=8 유지, **T=2 붕괴도 본문에**) | 수정 |
| 11 | novelty 현황 — 선행연구 이후 **남은 두 기여** | 수정 (§2.7) |
| 12 | 남은 한계와 다음 단계 | 유지 |

**발표 원칙 3가지 (이번 피드백의 근본 원인 대응)**
1. **비율을 쓰면 절대량을 반드시 같이 적는다.** "9.5× 증가"가 아니라 "24 B → 252 B (9.5×)".
2. **모든 배수에 무엇을 셌는지 한 줄로 붙인다.** 5.27×가 이 규칙 하나로 걸러졌을 것이다.
3. **지는 축을 먼저 적는다.** `PAPER_TABLE_COMPARISON.md` §5가 이미 그렇게 쓰여 있다 —
   내부 문서의 정직함이 발표까지 오지 못한 게 이번 문제의 핵심이다.

---

## 7. 이 계획이 실패하는 경우 — 미리 정해둘 것

| 시나리오 | 확률 | 대응 |
|---|---|---|
| E16에서 배수가 1.1×로 축소 | **높음** | 에너지를 헤드라인에서 내리고 **저-T 견고성 + 등정확도 op 효율**로 축 이동 |
| E18에서 attention이 net < 0 | **중간** | 주장 범위를 "비선형 활성함수"로 좁힘. ViT 예산의 68–70%를 잃으므로 전체 에너지 주장 철회 |
| E15에서 우리-MBE가 T=8을 버팀 | **중간** | 저-T 주장을 논문 표 인용으로만 축소. novelty가 N_j/T_j 배분 하나로 줄어듦 |
| 셋 다 나쁨 | 낮음 | 남는 것: **등정확도 op 층위 10/10 스파이크 승 + compare 중앙값 5.94× + 규칙의 4축 불변성**. 이건 여전히 논문 한 편이 되지만 **에너지 논문이 아니라 근사 효율 논문**이다 |

> 마지막 줄이 중요하다. **세 실험이 다 나빠도 결과는 남는다.** 다만 프레이밍이 바뀐다.
> 지금 프레이밍(에너지 우위)이 가장 공격받기 쉬운 쪽인데, 그게 헤드라인이었다는 게
> 이번 피드백의 구조적 원인이다.

---

## 부록 — 근거 파일

| 주장 | 파일 |
|---|---|
| 5.27× 해부, 엄격 계상 2.871 mJ, 클래스별 내역 | `results/energy_ratio_vitm.json` |
| MBE 대칭 계상 (미커밋) | `experiments/energy_ratio_vitm.py` `mbe_all_op()` |
| op별 스파이크/바이트 비율, matched 수 | `results/op_pareto_merged_wins.md`, `results/op_pareto_merged.json` |
| MBE 백엔드 저-T op 런 (T=4/8/16 각 50) | `results/op_pareto_merged.json` `meta.backends` |
| 메모리 절대량, dataflow bracket | `results/e7_memory_cost.json`, `experiments/memory_cost_model.py` |
| 파라미터 3규약 | `results/e5_param_model.json` `conventions` |
| 뉴런 층위 MBE vs PASN | `results/neuron_compare.json` |
| 내부 금지 문장 목록, 지는 축 | `PAPER_TABLE_COMPARISON.md` §2.4 · §2.6 · §5 · §7 |
| 선행연구 (routing 철회) | `RELATED_WORK.md` §6b |
| 공유 basis 메모리 회수 | `results/shared_banks_report.md` |
| MBE arm 중단 결정 (E15와 충돌) | memory `pasn-positioning-decision` |
