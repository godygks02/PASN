# AGENTS.md — 역할과 쓰기 경계

이 파일이 존재하는 이유는 하나다: **역할을 "무엇을 쓸 수 있는가"로 자른다.**
단계 수나 전문성으로 나누면 경계가 말랑해지고, 말랑한 경계는 지켜지지 않는다.

## 원칙 네 개

1. **파일시스템이 유일한 진실원이다.** 에이전트끼리 대화로 결과를 넘기지 않는다.
   런은 파일을 떨어뜨리고, 다음 에이전트는 **그 파일만** 읽는다.
2. **권한 경계로 역할을 자른다.** 아래 표의 "쓰기 가능"이 전부다.
3. **검증은 실행과 다른 모델로 한다.** 자기 결과를 자기가 채점하지 않는다.
   → 이 리포에서 검증은 **Codex(`codex exec -s read-only`)** 로 나간다. 샌드박스가
   쓰기를 기계적으로 막으므로, 경계가 프롬프트 약속이 아니라 강제다.
4. **novelty 판단과 최종 인용 책임은 사람에게 있다.** 에이전트는 후보와 근거까지만.

## 경계표

| 역할 | 하는 일 | 쓰기 가능 | 금지 | 모델 |
|---|---|---|---|---|
| **planner** | 다음 실험 결정, 가설 카드 | `research/ideas/` | 코드 실행, 수치 인용, `PAPER_PLAN.md` 직접 수정 | Claude |
| **scout** | 문헌 검색, novelty 후보 | `research/refs/` | 리포 내 다른 경로, novelty **결론** | Claude |
| **runner** | 실험 코드 작성·실행 | `experiments/`, `results/`(신규만), `src/` | 기존 레코드 덮어쓰기, 결과를 요약해 넘기기 | Claude |
| **verifier** | 반증, 재현성, 지문 대조 | `research/verdicts/` | **모든 쓰기**(read-only 샌드박스) | **Codex** |
| **writer** | 원고 초안 | `research/manuscript/` | 인용 생성, 수치 창작, 레코드 없는 주장 | Claude |

`PASN_vault/`는 **모든 역할에서 읽기 전용이다.** 사용자의 기록 공간이고 git 밖이다.

`PAPER_PLAN.md`와 `NEXT_EXPERIMENTS.md`는 **사람이 승인해야 바뀐다.** planner는
`research/ideas/`에 카드를 쓰고, 승인되면 사람이 `PAPER_PLAN.md` §2로 승격한다.
카드 디렉터리가 두 번째 진실원이 되면 안 된다 — §2가 우선순위의 단일 출처다.

## 이 역할들이 실패하는 방식

경계만으로는 안 되고, 각 역할이 **어떻게 조용히 망가지는지**를 알아야 한다.
아래는 추상적 우려가 아니라 이 프로젝트에서 실제로 일어난 것들이다.

- **planner** — 반증 조건 없는 가설을 만들어 검증 단계를 무력화한다.
  *실제*: 사전등록 게이트를 놓친 뒤(0.187% > 0.14%) 밴드를 사후에 넓히고 싶어진다.
  카드에 반증 조건이 비면 카드를 완성하지 않는다.
- **scout** — 검색 결과가 없을 때 그럴듯한 논문을 지어낸다.
  DOI/arXiv ID가 없는 항목은 "미확인 후보"로 **격리 섹션**에 둔다. 본문에 못 올린다.
- **runner** — 실패한 실험을 성공으로 보고한다. 그리고 **빌드를 바꾸는 손잡이를
  레코드에 안 적는다.**
  *실제*: `pasn_beta=0.5` 기본값이 모든 GPT-2 레코드보다 뒤에 들어와, 헤드라인
  레코드의 빌드를 소급 판정할 수 없게 됐다(E0가 이걸 고치느라 생겼다).
  → 레코드는 `convert_cfg` 37필드를 전부 싣는다.
- **verifier** — 실행자와 같은 모델이라 같은 착각을 공유한다.
  → 그래서 Codex로 나간다. 같은 모델을 쓸 수밖에 없으면 최소한 실행 세션과
  컨텍스트를 공유하지 않는 새 세션에서, **원본 레코드만 보고** 판정한다.
- **writer** — 존재하지 않는 인용을 만든다. 그리고 **빌드가 다른 수치를 섞는다.**
  *실제*: `−0.14%`(no-beta 빌드)와 `53,888 B`(beta 빌드)가 한 문장에 들어갈 수 있다.
  → 수치를 쓸 때마다 출처 레코드 경로와 tag를 같이 적는다.

## 호출 방법

`.claude/agents/` 아래 정의가 있다. 라우팅에 맡겨도 되고 이름으로 불러도 된다.

검증만 별도 경로다 — Claude 서브에이전트가 아니라 외부 프로세스다:

```bash
python research/tools/verify.py results/freeze_e1.json --claim "ΔPPL −0.19%"
```

이 스크립트가 `codex exec -s read-only --output-schema`로 나가고, 판정을
`research/verdicts/`에 JSON으로 떨군다. Codex가 없거나 인증이 안 되어 있으면
스크립트가 그 사실을 말하고 멈춘다 — **조용히 Claude로 폴백하지 않는다.**
폴백이 필요하면 사람이 `--fallback-claude`를 명시해야 하고, 판정 파일에
`"cross_model": false`가 박힌다.

## 승인 게이트 — 사람이 서 있는 자리

에이전트가 넘어가면 안 되는 선이다.

1. **가설 → 실험**: 카드의 반증 조건을 사람이 승인해야 GPU를 쓴다.
2. **실험 → 주장**: 검증 판정이 `refuted`면 주장으로 올리지 않는다.
3. **주장 → 원고**: novelty 결론과 최종 인용은 사람이 확인한다.
4. **원고 → 투고**: 아래 체크리스트를 사람이 직접 통과시킨다.

## 투고 전 체크리스트 (사람이 직접)

- [ ] 모든 인용이 실제로 존재하고 내용이 주장과 일치하는가
- [ ] 논문의 모든 수치가 `results/`의 레코드로 역추적되는가 (`check_build.py` 통과)
- [ ] 모든 수치가 **같은 빌드·같은 arm·같은 T**에서 나왔는가
- [ ] novelty 주장을 사람이 직접 검증했는가
- [ ] 목표 학회의 AI 도구 사용 정책을 확인하고 필요한 선언을 했는가
