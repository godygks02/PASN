# 논문 표 대조 — 결과 · 얻은 것 · 남은 것

**단일 출처: "우리 수치가 논문의 어느 칸 옆에 놓이는가."** 2026-08-12 기준.

실험 우선순위는 `PAPER_PLAN.md`, 사이클 서사는 `results/RESULTS_2026-08_cycle.md`,
CV 상세는 `results/RESULTS_CV.md`. **이 문서는 대조표와 그 해석만 담는다.**

---

## 0. 인용 규칙 — 이 표를 쓰기 전에

1. **상대 변환 손실만 비교한다.** 절대 정확도/perplexity 는 **어느 칸에서도** 비교
   불가다 — ANN 이 다르고(체크포인트·fine-tune), 평가 레시피가 미공개이며, ImageNet
   에서는 우리 파이프라인이 공개값보다 0.8–1.0 pp 낮다.
2. **논문이 인쇄한 백분율은 절대 pp 다.** Table 1 "0.44%/0.64%", Table 2 "0.24%",
   Table 3 "0.35%" 가 전부 그렇다. **상대값은 우리가 다시 계산한 것**이고, 아래 표의
   논문 열은 전부 재계산값이다.
3. **프리즈 빌드 지문**: `53,888 B / 13,472 p / 339 prims`, `pasn_beta={"inv":0.5}`.
   `−0.14%` · `49,952` · `12,488` 은 **구 빌드**이므로 인용 금지.
4. **op 예산 비중은 운영점을 붙여야 한다**(arm · `T` · 모델 세 축).
5. **op 우위 범위는 한 arm 에서**(`pasn_rule`): 10/10, **1.23×–5.39×**.
6. **1-D 적합을 균일 그리드에서 채점하지 말 것**(분포에 따라 3–4 자릿수 다르다).
7. **논문의 `17.29%` 옆에 우리 수치를 놓지 말 것** — 그건 가중치 matmul 까지 세는
   **전체-네트워크** 계상이고 우리는 변환 op 만 센다 → §2.1.
8. **비용 배수는 어느 읽기·어느 반쪽인지 붙일 것** — G.4 의 `N` 은 basis 가 아니라
   **토큰**이고(§2.2), 총합 우위의 93.5% 가 **FP 곱셈 한 항**이다(§2.4).

---

## 1. 대조표 — 전부 논문 자신의 전역 `T=16`, 같은 op 집합

| 논문 표 | 셀 | **논문** | **우리** | 마진 | 상태 |
|---|---|---:|---:|---:|---|
| **Table 1** CV | ViT-B/16 × ImageNet | −0.527% | **−0.020%** | 0.51 pp | ✅ |
| **Table 1** CV | **ViT-M/16** *(논문의 그 모델)* | −0.745% | **−0.0024%** | **0.74 pp** | ✅ |
| Table 1 CV | VGG16 / ResNet34 | −1.68 / −0.74 pp | — | — | ❌ CNN, 범위 확인 필요 |
| **Table 2** NLU | RoBERTa-base × SST-2 | −1.09% | **+0.12%** | 1.21 pp | ✅ |
| **Table 2** NLU | RoBERTa-large × SST-2 | −0.25% | **−0.12%** | 0.13 pp | ✅ |
| Table 2 NLU | RoBERTa-base × MR **@ `T=16`** | −0.44% | −0.73% … −0.10% | — | 🟡 **판정 불가** (체크포인트 분산이 감쌈) |
| **Table 4** NLU | **RoBERTa-base × MR @ `T=8`** | **−43.87%** (89.39→50.17) | **−0.95%** | **42.9 pp** | ✅ **판정 가능** — §3.1 |
| Table 2 NLU | SST-5 / Subj | — | — | — | ❌ 공개 체크포인트 부재 |
| **Table 3** NLG | GPT-2 × Wiki-2 | +1.57% | **−0.181%** | 1.75 pp | ✅ |
| **Table 3** NLG | GPT-2 × Wiki-103 | +3.36% | **−0.196%** | **3.56 pp** | ✅ **최대** |
| **Table 4** 저-`T` | GPT-2 @ `T=8` | **41072** (ANN 22.65) | **+0.062%** | — | ✅ **질적 차이** |
| **Table X** | 함수근사 MSE vs `N` | per-function | 재현·능가 | — | ✅ |
| **Table XI** | op 별 발화율 | 7 프리미티브 | **6/7, 4.48–32.35×** | — | ✅ **프리즈 빌드 재측정 (08-13)** — §2.7. 교차모델 + `per basis` 읽기(§2.2) 단서는 유지 |
| **G.1** | `N`·`T` 하이퍼파라미터 | N=4/8, T=16 | 매칭 (N 은 우리가 적음) | — | ✅ |
| **G.4** 비용 | ViT-M/16 변환 op 에너지 | 1.413–1.915 mJ/img | **0.268 mJ/img** | **5.27–7.14×** | ✅ **신설 §2** |
| ~~G.4 Eq.28~~ | ~~`E_SNN/E_ANN` 17.29%~~ | 17.29% | — | — | ❌ **비교 불가** — 범위가 다름 (§2.1) |

**채울 수 있는 칸은 전부 찼다.** 남은 셋(CNN 2행, SST-5/Subj)은 **런 문제가 아니다** —
CNN 은 어텐션이 없어 변환 범위가 다르고, SST-5/Subj 는 쓸 만한 공개 fine-tune 이 없어
**우리 ANN 을 직접 학습해야** 하는데 그건 다른 프로젝트다.

### op 층위 (Table XI 와 별도, 우리 스윕)

**등정확도에서 10/10 op 스파이크 승, `1.23×–5.39×`**(`pasn_rule`, 승자 중앙값 3.27×).
**등정확도 `cmp` 도 10/10 승, 중앙값 5.94×**(§2.5). **바이트는 10/10 패**(0.11×–0.93×).

---

## 2. 비용 축 — 논문 G.4 와의 대조 *(2026-08-12 신설)*

논문이 인쇄하는 **유일한 전체-모델 비용 수치가 ViT-M/16 의 `E_SNN/E_ANN = 17.29%`**
(부록 G.4, Eq. 28)이고 E8-M 이 **같은 timm 체크포인트**를 변환했다. 그런데 그 칸은
직접 못 채운다 — 아래 두 함정 때문이다.

산출: `experiments/energy_ratio_vitm.py` → `results/energy_ratio_vitm.json`.
기하는 timm 조회(260 토큰 · d=512 · 12블록 · 8헤드),
ANN **10.747 GMAC/img → E_ANN 49.43 mJ/img**(`E_MAC` 4.6 pJ).

### 2.1 ⚠️ 17.29% 는 우리와 같은 양이 아니다

`E_AC/E_MAC = 19.57%` 이므로 17.29% 는 **`SOPs/FLOPs = 0.884`** 를 강제한다. 변환된
비선형 op 만으로는 **0.146**(as printed) / 0.198(per basis) — **4.5–6× 부족**하다.
→ **Eq. 28 은 가중치 matmul 까지 스파이크 연산으로 세는 전체-네트워크 계상이다.**
우리 변환기는 가중치 곱을 exact 로 남기고 세지 않는다. **유효한 비교는 변환 op 대
변환 op 뿐이다.**

### 2.2 ⚠️ G.4 의 `N` 은 basis 가 아니라 토큰이다

> *"T is the timestep, η is the average firing rate, **N is the number of tokens**,
> C is the number of token channels, and N_h is the number of heads"*

**`E_MBE` 에 basis 개수가 없다**(Algorithm 1 은 basis 를 `B` 로 따로 쓴다). 그런데
Eq.(5)–(8) 은 basis 마다 독립된 막전위·임계·Heaviside 발화를 준다 — `B` 개 basis 는
`B` 개 스파이크 열을 낸다. **두 읽기를 다 보고한다**: `as printed`(`T·η`) /
`per basis`(`T·η·B`). 🔴 이 정정이 [[Table XI 발화율 대조]] §1 의 귀속 문장을 바꾼다.

### 2.3 결과 — 변환 op 에너지 (같은 op 집합 · 전역 `T=16` · 같은 `E_AC`)

| | 스파이크/img | pJ/img | E_ANN 대비 |
|---|---:|---:|---:|
| MBE, **as printed** | 1570.5M | 1.413 mJ | 2.86% |
| MBE, **per basis** | 2128.2M | 1.915 mJ | 3.87% |
| **PASN, 논문 통화**(spikes×E_AC) | **297.9M** | **0.268 mJ** | **0.54%** |
| PASN, 엄격(cmp+router+poly 포함) | 297.9M | 2.871 mJ | 5.81% |

**논문 자신의 통화로 5.27×(as printed) / 7.14×(per basis).**

### 2.4 🔴 총합을 그대로 인용하면 안 된다 — 반쪽이 부호가 다르다

| | MBE | PASN | 비율 |
|---|---:|---:|---:|
| **FP 곱셈**(활성×활성) | 1468.5M | 100.7M | **14.58×** |
| 비선형 op, as printed | 102.0M | 197.2M | **0.52× 🔴 패** |
| 비선형 op, per basis | 659.8M | 197.2M | 3.35× |

**저쪽 변환 op 스파이크의 93.5% 가 FP 곱셈 하나**이고 총합 우위는 거기서 나온다.
**논문 공식을 문자 그대로 읽으면 비선형 연산자에서는 우리가 1.9× 진다.** 그리고 그
14.58× 는 저쪽 `E_FP = T²·η₁·η₂·MO`(MAC 당 1.77 스파이크, 우리 0.121)에 전적으로
의존하는데 **구현으로 검증하지 않은 모델 공식**이다.

### 2.5 cmp — 등정확도 op 층위에서 10/10 승

`cmp` 는 양쪽 다 `basis × T`(원소당)이고 **G.4 는 어느 쪽에도 청구하지 않는다.**

| operator | nrmse | MBE `N·T` | PASN `N·T` | 비율 |
|---|---:|---:|---:|---:|
| tanh | 7.0e-02 | 32 | 2.0 | **16.00×** |
| gelu | 4.9e-03 | 128 | 10.4 | **12.30×** |
| softmax | 5.2e-03 | 128 | 13.1 | 9.74× |
| attention | 1.6e-02 | 128 | 16.8 | 7.62× |
| silu | 5.2e-03 | 96 | 16.2 | 5.94× |
| exp2 | 3.0e-03 | 48 | 11.1 | 4.32× |
| inv | 3.3e-03 | 48 | 13.8 | 3.48× |
| fp_multiply | 1.8e-03 | 64 | 36.6 | 1.75× |
| layernorm | 1.5e-02 | 32 | 19.3 | 1.65× |
| invsqrt | 7.1e-03 | 32 | 32.0 | 1.00× |

**10/10, 중앙값 5.94×, 범위 1.00–16.00×**(`pasn_rule`).

⚠️ **네트워크 층위 cmp 비율을 만들지 말 것.** MBE 쪽은 비선형 op **6종**만 셀 수 있고
(`E_FP` 가 피연산자 복원 구조를 명시하지 않는다) 우리 측정치는 FP 곱셈 경로를 포함한
**7종**이다(우리 스파이크의 34%). 그 둘을 나누면 **1.06× 라는 가짜 동률**이 나온다 —
2026-08-12 에 실제로 한 번 만들었다가 폐기했다.

### 2.6 우리 엄격 계상의 구성 — 취약점이 여기 있다

| 클래스 | ops/img | mJ/img | 비중 |
|---|---:|---:|---:|
| **cmp** | 1562.7M | 1.406 | **49.0%** |
| ac | 595.9M | 0.536 | 18.7% |
| mac | 113.9M | 0.524 | 18.2% |
| **router** | 367.0M | 0.330 | **11.5%** |
| poly | 16.2M | 0.015 | 0.5% |
| **bitop** | 61.2M | **0.000** | **0.0%** ← 지수 비트 읽기는 공짜로 계상됨 |

**항상 켜져 있는 두 클래스(cmp + router)가 60.5%** — MoE 유비가 예고한 게이팅
오버헤드가 실제로 여기 있다. 단 **라우터 6 ops/원소는 후하게 잡은 값이다**(뱅크 입력
affine 은 IEEE 지수 덮어쓰기 = bitop, clamp 는 saturating 인덱스로 접힘). 2 로 내리면
11.5% → 약 4% 인데 **모델링이지 측정이 아니므로 sensitivity 로만 쓴다.**
쓸 문장은 **"라우터에 원소당 6 연산을 청구하고도 11.5%"** 다.

### 2.7 Table XI 재측정 — 프리즈 빌드 · `gpt2-medium` · epochs 300 *(2026-08-13)*

기존 표는 **08-02 · gpt2-small · epochs 120 · `beta` 이전** 빌드라 인용 불가였다.
`firing_rates.py --model gpt2-medium --epochs 300 --t-fixed 16` 로 재측정
(`ConvertConfig` 기본값이 프리즈 빌드와 일치함을 확인) → `results/firing_rates_medium.json`.

| 논문 op | 발화율 논문 | 우리 | spikes/elem 논문 | 우리 | **비율** | 구 표 |
|---|---:|---:|---:|---:|---:|---:|
| `2^x` | 46.94% | 23.38% | 60.08 | 4.67 | **12.86×** | 9.31× |
| `LayerNorm_1/x_identity` | 38.46% | 6.34% | 49.23 | 1.52 | **32.35×** | 10.34× |
| `GELU` | 38.22% | 24.17% | 24.46 | 3.90 | **6.27×** | 3.86× |
| `LayerNorm_input_identity` | 27.61% | 6.68% | 35.34 | 1.61 | **21.96×** | 7.35× |
| `1/√x` | 25.27% | 45.12% | 32.35 | 7.22 | **4.48×** | 3.08× |
| `Attention_score` | 8.31% | 6.68% | 10.64 | 1.61 | **6.61×** | 2.22× |
| **`1/x`** | 3.74% | 35.47% | 4.79 | 5.68 | **0.84× 🔴 패** | 0.26× |

**6/7 유지, 범위는 `2.2–10.3×` → `4.48–32.35×`.** 승자 전부가 개선됐다.

🔴 **`1/x` 은 여전히 진다 — 0.26× → 0.84× 로 좁혀졌을 뿐이다.**
⚠️ **실험 12 의 "1.14× 로 논문도 이긴다" 를 이 표에 옮기지 말 것.** 그건 **고립 op**
측정이고 이건 **네트워크 안**이다(인덱스가 경고한 그 구분). 프리즈 빌드에서 in-network
로는 **`1/x` 이 유일한 패배 행으로 남아 있다.** E0 이 `beta` 를 채택한 근거였던
"op 9/10 → 10/10" 은 고립 op 표의 이야기이지 Table XI 대조가 아니다.

📌 Table XI 에 대응 없는 두 행: `matmul av` 4.67, `softmax idn` **4.32**
(구 기록 6.96 은 `inv_S` 수정 전 값 — 정정 반영됨).
⚠️ **교차모델 단서 유지**: Table XI 는 ViT-M/16 추론 중 측정, 우리는 GPT-2.

---

## 2.8 ★ 라우터 ablation — 저-`T` 붕괴의 **원인**을 우리 시스템 안에서 재현 *(E14, 2026-08-13)*

**이 표가 이 연구의 메커니즘 주장이다.** 지금까지 저-`T` 우위는 전부 cross-paper
였고(`mbe` 백엔드 저-`T` 런이 **0건**, C9 는 E2 에서 삭제), *"저쪽이 저 `T` 로
튜닝을 안 한 것"* 과 구분할 수 없었다. `pasn_e_min` 을 올려 라우터를 접으면
**깨진 전역-MBE arm 을 거치지 않고** 라우팅만 제거할 수 있다.

**사전등록한 예측** (E14 큐 문서, 런 이전 기록): *"`T=16` 은 완만히, `T=8` 은 급격히
저하. 둘이 같으면 메커니즘 주장 실패."*

**격자 완성 (5/5, 8h42m).** `e_min` 은 **활성함수·softmax 라우터**를 접는다
(항등원은 `pasn_id_e_min` 이 따로 잡으며 **이 스윕은 건드리지 않았다** — §한계).

| `e_min` | **활성 뱅크** | softmax 뱅크 | **`T=16` ΔPPL** | **`T=8` ΔPPL** | 스파이크 `T=8` | bytes |
|---:|---:|---:|---:|---:|---:|---:|
| **−3** (프리즈) | **21** | 3 | **−0.181%** | **−0.017%** | **1,713,431** | 53,888 |
| **4** | 7 | 1 | +2.535% | **+1.120%** | 1,843,225 | 38,144 |
| **12** | **3** | 1 | +10.083% | **+12,582.6%** 🔴 | 1,914,372 | 30,272 |

### ⚠️ 정정 — "매끄러운 상호작용" 이 아니라 **절벽**이다

세 점만 봤을 때 *"저 `T` 에서 1,248× 더 아프다"* 로 적었는데, **중간점이 그 서술을
반증한다.** `e_min=4`(활성 7뱅크)에서는 **`T=8` 이 `T=16` 보다 오히려 낫다**
(+1.12% vs +2.535%). 뒤집히는 것은 **활성 뱅크가 7 → 3 으로 떨어질 때뿐**이고,
거기서 `T=16` 은 +10% 로 버티는데 `T=8` 은 **ppl 2752.9 로 무너진다.**

> **정확한 문장**: 라우팅 깊이와 timestep 예산은 **서로 대체재**다. 뱅크가 충분하면
> `T=8` 이 `T=16` 만큼(때로는 더) 좋다. **뱅크를 어떤 바닥 아래로 깎으면 `T=16` 은
> 우아하게 저하되고 `T=8` 은 절벽에서 떨어진다.**
>
> 이것이 **논문이 `T=16` 을 필요로 하고 `T=8` 에서 죽는 이유**다 — 저쪽은 용량을
> 전부 timestep 으로만 지불한다(`N=4/8` 고정, 구간 분할 없음).

📌 **저쪽의 실패 모드가 우리 안에서 재현된다.** 논문 GPT-2 는 `T=8` 에서 22.65 →
**41072**(+181,233%); 활성 라우터만 3뱅크로 접은 우리 빌드는 21.71 → **2752.9**
(+12,583%). **같은 종류의 파국**이 다른 것은 전부 고정한 채 나온다 →
*"저쪽이 튜닝을 안 했다"* 와 *"전역 용량이 저 `T` 에서 깨진다"* 가 갈린다.

📌 **저쪽의 실패 모드가 우리 안에서 재현된다.** 논문 GPT-2 는 `T=8` 에서 22.65 →
**41072**(+181,233%); 라우팅만 제거한 우리 빌드는 21.71 → **2752.9**(+12,583%).
**같은 종류의 파국**이 다른 것은 전부 고정한 채 **라우팅 하나를 빼서** 나온다.
→ *"저쪽이 튜닝을 안 했다"* 와 *"전역 용량이 저 `T` 에서 깨진다"* 가 이제 갈린다.

### 거래가 아니라 지배다

접은 빌드는 `T=8` 에서 **스파이크를 더 쓰고도**(1.91M vs 1.71M) 파괴된다.
그리고 `T=16` 으로 올려 살려내도 **라우팅+`T=8` 의 3.04× 스파이크**(5.20M vs 1.71M)를
쓰고 **+10.08%** 다. **라우팅된 `T=8` 이 두 축 모두에서 접힌 빌드를 지배한다.**
라우팅의 대가는 저장 **1.78×**(53,888 vs 30,272) 하나뿐이다.

### ⚠️ 부수: arm 결함 수리 + `T=8` 값 갱신

`e14-emin-3-t8` 은 **`unif` arm**(`--pasn-t-fixed 8`)이라 §3 의 `alloc` 곡선과 arm 이
다르던 문제를 닫는다. **`unif` 가 더 좋다: −0.0173% vs `alloc` +0.062%.**
저-`T` 를 인용할 때는 **`unif` 로 통일**한다(ViT·RoBERTa 스크립트는 `--pasn-t-fixed`
만 노출하므로 자동으로 `unif`).

### ⚠️ 한계 — 이것은 **부분** ablation 이다

1. **항등원 라우터는 안 건드렸다.** `pasn_e_min` 은 활성함수·softmax 만 접고,
   항등원은 `pasn_id_e_min`(기본 −6) 이 따로 잡는다. 로그에서 `layernorm` 뱅크가
   세 arm 모두 **3 으로 불변**인 것이 그 증거다. 🔴 **하필 논문이 자기 실패를 진단한
   축이 항등원이다**(*"wider identity mapping range [0,62]"*). 우리는 **그 축을 건드리지
   않고도** 파국을 재현했다 — 주장으로는 더 강하지만 **같은 축의 재현은 아니다.**
   완전한 ablation 은 `pasn_id_e_min` 도 쓸어야 한다.
2. **적합 실패와 완전히 분리되지 않았다.** 다만 순수 적합 실패 가설과 **맞지 않는
   증거가 둘**이다: (a) 같은 빌드가 `T=16` 에서 +10.08% 로 **살아 있고**, (b) `e_min=4`
   에서는 **`T=8` 이 `T=16` 보다 낫다**. 적합이 깨진 것이라면 `T` 와 무관해야 한다.
   **이 논증을 논문에 적을 것.**
3. **`T` 두 점, `e_min` 세 점**이다. 절벽의 위치(7→3 사이)는 특정되지 않았다.

---

## 3. 가장 강한 결과 — Table 4

논문이 **파국적으로 지는 유일한 표**이고, 우리가 가장 크게 앞서는 자리다.

| `T` | 논문 GPT-2 (Wiki-103) | 우리 (Wiki-2) |
|---:|---|---|
| 16 | 23.41 → +3.36% | **−0.187%** |
| 12 | 33.56 → **+48.2%** | *(미측정)* |
| 10 | 11992 → **+52,845%** | *(미측정)* |
| **8** | **41072 → +181,233%** | **+0.062%** |
| 4 / 2 | *(논문 미측정)* | +2.137% / **+60.97%** |

**`T=8` 은 논문이 보고하는 최저 `T` 이고, 거기서 Table 4 의 다섯 모델이 전부 무너진다**
— ViT-B/16 83.44→**0.12**, ViT-M/16 85.95→**1.17**, RoBERTa 89/91→**~50**,
GPT-2 22.65→**41072**. **저 `T` 취약성은 방법 전반의 성질이다.** 우리는 밴드 안이다.

⚠️ **서사를 정확히**: ~~"우리는 안 깨진다"~~ → **"저쪽이 깨지는 `T` 에서 우리는
무손실이고, 우리는 저쪽이 재지도 않은 `T=2` 에서 무너진다."**
⚠️ **`T` 를 가로질러 비교 금지** — 논문 `T=16`(+1.57%) vs 우리 `T=4`(+2.14%)는
승패가 아니라 **교환점**(스파이크 5.44× 덜 쓰고 0.57 pp 더 잃음).
⚠️ 코퍼스: Table 4 는 Wiki-103 단어수준, 우리 곡선은 Wiki-2 raw.

### 3.1 📌 저-`T` 가 판정 불가 셀을 판정 가능하게 만든다 *(2026-08-13, 로컬 CPU)*

**MR 은 `T=16` 에서 판정 불가다** — 우리 두 체크포인트가 논문을 감싼다
(`…rotten_tomatoes` −0.738% / `…rotten-tomatoes` −0.106%, 논문 −0.44%, 분산 0.63 pp).

**`T=8` 에서는 갈린다:**

| | ANN | `T=8` | 상대 |
|---|---:|---:|---:|
| 논문 RoBERTa-B (Table 4) | 89.39 | **50.17** | **−43.87%** |
| **우리** (`textattack/…rotten_tomatoes`) | 89.02 | **88.18** | **−0.95%** |

**마진 42.9 pp = 체크포인트 분산의 68×.** 효과 크기가 잡음보다 두 자릿수 크므로
**어느 체크포인트를 골라도 결론이 같다.** `T=16` 에서 못 쓰던 셀이 `T=8` 에서 살아난다.

📌 **`T=16`→`T=8` 의 대가가 0.21 pp 다** (−0.738% → −0.95%). 같은 구간에서 논문은
−0.44% → −43.87% 로 간다.

⚠️ 우리 ANN 은 **제3자 fine-tune** 이라 절대값 비교 불가 — 상대값만.
⚠️ arm 은 `unif`(`--pasn-t-fixed 8`), 논문의 전역 `T` 와 같은 것.
📌 op 예산이 또 움직인다 — **LayerNorm 42.1%** (GPT-2 10.0% / ViT-M 18.9%),
**라우터가 에너지의 19.6%** (ViT-M 11.5%). §2.6 의 운영점 의존성이 모델 축에서도 참.

---

## 4. 대조 과정에서 **논문에 대해** 알아낸 것

| # | 발견 | 쓰임새 |
|---|---|---|
| **1** | **Table 4 의 `T` 축은 `8/10/12/16`** — `2/4/8/16` 으로 **추정될 뻔했다**. 논문은 `T=4`·`T=2` 를 **아예 안 잰다** | 그 추정으로 표를 만들었으면 헤드라인이 통째로 틀렸다 |
| **2** | **본문의 "near-optimal at T=12" 는 GPT-2 엔 거짓** — 33.56 = **+48.2%** (ViT·RoBERTa 에는 참) | 그 문장을 그대로 인용하면 안 된다 |
| **3** | **G.1: *"all learnable parameters in the MBE neuron are fixed as [20]"*** — 항등원은 **학습 자체를 안 한다**. 본문도 *"D … shared across the entire network"* | 🔴 **우리 tied 항등원이 차별점이 아니다.** 339 중 218 이 항등원인데 저쪽 계산으론 전부 공짜 |
| **4** | **Table X 의 `inv` 이 비단조** — N=4(1.1e-3) → N=6(**2.2e-3**) | 적합의 `N` 비단조성이 **우리만의 병리가 아니다**(우리가 훨씬 심할 뿐) |
| **5** | **22.34 vs 22.65 는 `<unk>` 다** — raw test split 은 **sha256 동일**, 단어수준만 6.31% vs 1.03% | **논문은 단어수준 릴리스를 썼다**(연역). raw 로 -103 을 돌리면 그 칸이 안 채워진다 |
| **6** | **저쪽이 자기 저-`T` 실패를 *"wider identity mapping range [0,62]"* 로 진단** | **지수 라우터가 겨냥하는 바로 그 자리 — 서론 동기 문단** |
| **7** | 인쇄된 백분율이 **전부 절대 pp** (Table 1·2·3) | 상대값은 우리가 재계산해야 한다 |
| **8** | **G.4 의 `E_MBE` 에 basis 개수가 없다** — `N` 은 *토큰 수*, Algorithm 1 은 basis 를 `B` 로 따로 쓴다. 그런데 Eq.(5)–(8) 은 basis 마다 독립 발화를 준다 | 🔴 **저쪽 에너지 모델이 자기 뉴런을 4–8× 적게 청구한다.** 두 읽기를 다 보고하고, *"논문 자신의 식"* 은 `as printed` 에만 붙인다 → §2.2 |
| **9** | **Eq. 28 의 17.29% 는 전체-네트워크 계상** — `SOPs/FLOPs = 0.884` 를 강제하는데 변환 op 만으로는 0.146 | 우리 수치와 **다른 양**이다. 나란히 놓으면 `(+0.35)` 함정의 재판 → §2.1 |
| **10** | **저쪽 변환 op 비용의 93.5% 가 FP 곱셈 하나** — `E_FP = T²·η₁·η₂·MO`, MAC 당 1.77 스파이크 | 우리 비용 우위가 사실상 이 한 항에서 나온다. **검증 안 된 모델 공식**이므로 단서 필수 → §2.4 |

---

## 5. 대조 과정에서 **우리에 대해** 알아낸 것

### 방법이 안정적이라는 증거 — 네 축에서 규칙 출력 불변

| 축 | 섭동 | 결과 |
|---|---|---|
| 캘리브레이션 draw (E12) | disjoint 3벌, 정의역 3–4% 이동 | **19/19 뱅크 동일**, 저장 동일, 스파이크 0.011% |
| **코퍼스** (E3) | 49× 큰 다른 코퍼스, `<unk>` 치환 | **빌드 바이트 동일**, 스파이크 1.001× |
| **적합 epochs** (E8) | 50 → 300 | **저장 바이트 동일** (손실은 48–192× 개선) |
| 하드웨어 | 3개 박스 | **지문 바이트 동일**, ΔPPL 잡음 **0.0064 pp** |

> **"예산을 손튜닝이 아니라 푼다"의 실증이다.** 튜닝한 상수는 이렇게 재현되지 않는다.

### 저장이 모델 **폭**에 무관하다

GPT-2(d=1024, 24층) 339 prims / 13,472 p · ViT-B(768, 12층) 171 / 6,500 ·
**ViT-M(512, 12층) 171 / 6,404**. → **`O(사이트 수)`, 사이트는 깊이 비례.**
*"파라미터를 추가하는 방법 아니냐"* 에 **넓어질수록 상대 오버헤드가 준다**는 답.

### 우리가 지는 축 — 먼저 적을 것

| 축 | 상태 |
|---|---|
| **파라미터** | 3 규약 중 **2 패**. G.1 원문대로면 **5,929 vs 13,472 = 0.44×** |
| **메모리(데이터 이동)** | 최악 dataflow 에서 **패**(연산의 1.8%). tensor-stationary 에선 4.19× 승 |
| **비선형 op 스파이크** (as printed 읽기) | **0.52× 패** — 우리 우위는 FP 곱셈에서만 나온다 (§2.4) |
| **게이팅 오버헤드** | 우리 엄격 계상의 **60.5%** 가 항상 켜져 있는 cmp+router. MoE 유비가 예고한 대가 (§2.6) |
| **스케일** | 345M / 86M / 64M. NLSpike 는 8B급 4종 |
| **절대 정확도** | 모든 칸에서 비교 불가. ImageNet 은 우리 파이프라인이 **0.8–1.0 pp 낮다**(미규명) |
| **메커니즘 주장** | **C9 삭제** — 우리 전역 MBE 재현이 발산하는데 "전역은 안 된다"와 "우리가 못 맞춘다"를 못 가른다 |

---

## 6. 남은 실험

### 🔴 제출 전 반드시

| # | 항목 | 비용 | 왜 |
|---|---|---|---|
| **E9** | **더 큰 모델 1점** (GPT-2-large / Pythia-1.4B 급) | **~5 h GPU** | **가장 유력한 리젝 사유가 스케일**이고, 이게 부분적으로 막는다. **비용 대비 효과가 가장 큼** |
| ~~**문헌 조사**~~ | ~~지수/binade 라우팅 선행연구~~ | — | 🔴 **1차 완료 2026-08-13 — 결과가 음성이다.** 지수→구간별 근사는 **선행 기술**(BBAL arXiv:2504.15721, US 11163533), 오차 기반 비균등 분할도 **선행**(NPE arXiv:2104.06535 — **near-zero GELU 진단까지 선점**), SNN MoE 라우팅도 존재(SEMM NeurIPS'24). **메커니즘을 기여로 못 쓴다** → `RELATED_WORK.md` §6b. ✅ **FQA·NPE 원문 정독 완료(08-13)**: *"오차 제약에서 구간별 용량을 푼다"* 는 **PPA 문헌의 표준 관행**이라, `RELATED_WORK.md` §2 의 중심 주장이 **SNN 두 편에 대해서만** 신규다. 🔴 **[24] 추적 완료(08-13) — Lee et al. TVLSI 2009 «Hierarchical Segmentation» 이 결정적 선행연구다**: **2의 거듭제곱(=binade) 분할** + **leading-zero 검출로 세그먼트 주소 계산**(주소 로직 지연 최소화가 명시적 동기) + **오차 허용치에서 세그먼트 수를 자동으로 푸는** 것까지 전부. **"라우팅이 공짜다" 차별점은 철회.** 살아남는 것은 둘 — **스파이크 도메인 자원(`N_j`,`T_j`)** 과 **저-`T` 실패 모드**. → `RELATED_WORK.md` §6b |
| **라우터 ablation** | `pasn_e_min` 스윕 @ `T=8` | 6–8h GPU | 🔴 **§6b 이후 선택이 아니라 필수.** 저-`T` 우위가 전부 cross-paper 이고(`mbe` 백엔드 저-`T` 런이 **0건**), C9 는 이미 삭제됐다. 이게 없으면 **알려진 메커니즘 + 설명 안 되는 승리** |
| **E10 재확인** | NLSpike 원문 대조 | 논문 읽기 | `RELATED_WORK.md` 의 ⓝ 표시가 전부 **2차 인용** |
| **대칭 비용 모델** | 양쪽에 cmp·readout 을 같은 규칙으로 | 로컬 | 지금은 **저쪽 통화**(우리 유리, 라우터 미계상)와 **우리 통화**(불리, 저쪽 cmp 미계상) 둘뿐이다. 막는 건 저쪽 **FP 곱셈 피연산자 복원 구조 미상** 하나 → §2.5 |
| **Table XI 재측정** | 프리즈 빌드 + `gpt2-medium` + epochs 300 | 로컬 1회 | 현재 표는 **08-02·gpt2-small·epochs 120·`beta` 이전** 빌드다. `1/x` 패배는 이미 뒤집혔다(0.26×→1.14×, 고립 op 기준) |

### 🟡 하면 좋은 것

| # | 항목 | 비용 | 왜 |
|---|---|---|---|
| E12 2단계 | ΔPPL 오차막대 | GPU 1–2점 | 잡음 바닥 0.0064 pp 가 핵심 질문엔 이미 답함 → 우선순위 낮음 |
| CNN 행 | VGG16 / ResNet34 | 배선 확인 + GPU | Table 1 을 통째로 채운다. 어텐션이 없어 범위가 다름 |
| ImageNet 격차 | 0.8–1.0 pp 원인 규명 | 로컬 | 절대값 비교를 열 수 있다 |
| E4 재시도 | `spike_budget` 스윕 | GPU | 현재 S 판정은 "예산 안 준 S" 한 점 |

### ⬜ 다음 사이클

**softmax→matmul 융합** — 남은 최대 에너지 표적(어텐션이 GPT-2 예산의 81.8%,
`av_matmul` 이 최대 사이트 오차). **수치를 크게 바꿔 전면 재평가**를 부르고, **값어치가
모델 의존**임이 밝혀졌다(ViT 에선 어텐션이 68–70%).

---

## 7. 판정 — 이 표로 무엇을 주장할 수 있나

**쓸 수 있는 문장:**

> 구간별 예산을 고정하지 않고 **푸는** training-free 변환 뉴런. 논문 자신의 전역
> `T=16` 과 같은 op 집합에서 **CV·NLU·NLG 세 모달리티 모두** 변환 손실이 낮고,
> **논문이 보고하는 최저 `T` 에서 저쪽 다섯 모델이 전부 무너지는 동안 무손실**이며,
> 그 예산 규칙이 캘리브레이션·코퍼스·적합·하드웨어 네 축에 불변이다.

**비용 축에서 쓸 수 있는 문장** *(2026-08-12 신설)*:

> 논문 자신의 모델(ViT-M/16)·전역 `T=16`·에너지 상수로, **두 방법이 실제로 교체하는
> 연산자에서** PASN 은 스파이크 에너지를 **5.3× 적게** 쓴다(ANN 의 0.54% vs 2.86%).
> 등정확도 op 층위에서는 **10/10 op 에서 스파이크 1.23–5.39×, 비교 연산 중앙값
> 5.94× 적다**. 라우터의 지수-비트 판독은 계상에서 **0 pJ** 이고, 그 주변 산술에
> 원소당 6 연산을 청구하고도 우리 에너지의 **11.5%** 다.

**쓰면 안 되는 문장:**

* ~~"MBE 를 전방위로 이겼다"~~ — 파라미터·메모리는 진다.
* ~~"전역 multi-basis 는 구조적으로 불가능하다"~~ — **C9, 삭제됨**.
* ~~"메모리를 아낀다"~~ — 논문이 이미 항등원을 고정·공유한다.
* ~~절대 정확도/perplexity 를 나란히 놓는 것~~ — 어느 칸에서도 불가.
* ~~"논문 17.29% 대비 우리 0.54%"~~ — **다른 양이다** (§2.1).
* ~~"비선형 연산을 더 싸게 한다"~~ — `as printed` 읽기에서는 **1.9× 진다**. 우리
  비용 우위는 **FP 곱셈 한 항**에서 나온다 (§2.4).
* ~~"라우터는 공짜다"~~ — **비트 판독만** 공짜다. 라우터 전체는 11.5%.
* ~~네트워크 층위 `cmp` 비율~~ — 두 계상의 op 집합이 다르다 (§2.5).

---

관련: `PAPER_PLAN.md`(실험 우선순위) · `results/RESULTS_2026-08_cycle.md`(사이클) ·
`results/RESULTS_CV.md`(CV 상세) · `RELATED_WORK.md`(포지셔닝) ·
`experiments/energy_ratio_vitm.py` + `results/energy_ratio_vitm.json`(§2 전체) ·
`PASN_vault/60 - 연구일지/00 - 연구일지 인덱스.md`(실험 기록) ·
`PASN_vault/50 - Reference/MBE 논문 published 설정과 수치.md`(기준선 원문).
