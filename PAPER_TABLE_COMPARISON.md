# 논문 표 대조 — 결과 · 얻은 것 · 남은 것

**단일 출처: "우리 수치가 논문의 어느 칸 옆에 놓이는가."** 2026-08-12 기준.

실험 우선순위는 `PAPER_PLAN.md`, 사이클 서사는 `results/RESULTS_2026-08_cycle.md`,
CV 상세는 `results/RESULTS_CV.md`. **이 문서는 대조표와 그 해석만 담는다.**

---

## 0. 인용 규칙 — 이 표를 쓰기 전에

1. **상대 변환 손실만 비교한다.** 절대 정확도/perplexity 는 **어느 칸에서도** 비교
   불가다 — ANN 이 다르고(체크포인트·fine-tune), 평가 레시피가 미공개이며, ImageNet
   에서는 우리 파이프라인이 공개값보다 0.8–1.0 pp 낮다.
2. **논문이 인쇄한 백분율은 절대 pp 다.** Table 1 "0.44%/0.64%", Table 2 "0.24%",
   Table 3 "0.35%" 가 전부 그렇다. **상대값은 우리가 다시 계산한 것**이고, 아래 표의
   논문 열은 전부 재계산값이다.
3. **프리즈 빌드 지문**: `53,888 B / 13,472 p / 339 prims`, `pasn_beta={"inv":0.5}`.
   `−0.14%` · `49,952` · `12,488` 은 **구 빌드**이므로 인용 금지.
4. **op 예산 비중은 운영점을 붙여야 한다**(arm · `T` · 모델 세 축).
5. **op 우위 범위는 한 arm 에서**(`pasn_rule`): 10/10, **1.23×–5.39×**.
6. **1-D 적합을 균일 그리드에서 채점하지 말 것**(분포에 따라 3–4 자릿수 다르다).

---

## 1. 대조표 — 전부 논문 자신의 전역 `T=16`, 같은 op 집합

| 논문 표 | 셀 | **논문** | **우리** | 마진 | 상태 |
|---|---|---:|---:|---:|---|
| **Table 1** CV | ViT-B/16 × ImageNet | −0.527% | **−0.020%** | 0.51 pp | ✅ |
| **Table 1** CV | **ViT-M/16** *(논문의 그 모델)* | −0.745% | **−0.0024%** | **0.74 pp** | ✅ |
| Table 1 CV | VGG16 / ResNet34 | −1.68 / −0.74 pp | — | — | ❌ CNN, 범위 확인 필요 |
| **Table 2** NLU | RoBERTa-base × SST-2 | −1.09% | **+0.12%** | 1.21 pp | ✅ |
| **Table 2** NLU | RoBERTa-large × SST-2 | −0.25% | **−0.12%** | 0.13 pp | ✅ |
| Table 2 NLU | RoBERTa-base × MR | −0.44% | −0.73% … −0.10% | — | 🟡 **판정 불가** |
| Table 2 NLU | SST-5 / Subj | — | — | — | ❌ 공개 체크포인트 부재 |
| **Table 3** NLG | GPT-2 × Wiki-2 | +1.57% | **−0.181%** | 1.75 pp | ✅ |
| **Table 3** NLG | GPT-2 × Wiki-103 | +3.36% | **−0.196%** | **3.56 pp** | ✅ **최대** |
| **Table 4** 저-`T` | GPT-2 @ `T=8` | **41072** (ANN 22.65) | **+0.062%** | — | ✅ **질적 차이** |
| **Table X** | 함수근사 MSE vs `N` | per-function | 재현·능가 | — | ✅ |
| **Table XI** | op 별 발화율 | 7 프리미티브 | **6/7, 2.2–10.3×** | — | ✅ 교차모델 단서 |
| **G.1 / G.4** | `N`·`T` / 에너지 상수 | N=4/8, T=16 / 0.9 pJ | 매칭 (N 은 우리가 적음) | — | ✅ |

**채울 수 있는 칸은 전부 찼다.** 남은 셋(CNN 2행, SST-5/Subj)은 **런 문제가 아니다** —
CNN 은 어텐션이 없어 변환 범위가 다르고, SST-5/Subj 는 쓸 만한 공개 fine-tune 이 없어
**우리 ANN 을 직접 학습해야** 하는데 그건 다른 프로젝트다.

### op 층위 (Table XI 와 별도, 우리 스윕)

**등정확도에서 10/10 op 스파이크 승, `1.23×–5.39×`**(`pasn_rule`, 승자 중앙값 3.27×).
**바이트는 10/10 패**(0.11×–0.93×).

---

## 2. 가장 강한 결과 — Table 4

논문이 **파국적으로 지는 유일한 표**이고, 우리가 가장 크게 앞서는 자리다.

| `T` | 논문 GPT-2 (Wiki-103) | 우리 (Wiki-2) |
|---:|---|---|
| 16 | 23.41 → +3.36% | **−0.187%** |
| 12 | 33.56 → **+48.2%** | *(미측정)* |
| 10 | 11992 → **+52,845%** | *(미측정)* |
| **8** | **41072 → +181,233%** | **+0.062%** |
| 4 / 2 | *(논문 미측정)* | +2.137% / **+60.97%** |

**`T=8` 은 논문이 보고하는 최저 `T` 이고, 거기서 Table 4 의 다섯 모델이 전부 무너진다**
— ViT-B/16 83.44→**0.12**, ViT-M/16 85.95→**1.17**, RoBERTa 89/91→**~50**,
GPT-2 22.65→**41072**. **저 `T` 취약성은 방법 전반의 성질이다.** 우리는 밴드 안이다.

⚠️ **서사를 정확히**: ~~"우리는 안 깨진다"~~ → **"저쪽이 깨지는 `T` 에서 우리는
무손실이고, 우리는 저쪽이 재지도 않은 `T=2` 에서 무너진다."**
⚠️ **`T` 를 가로질러 비교 금지** — 논문 `T=16`(+1.57%) vs 우리 `T=4`(+2.14%)는
승패가 아니라 **교환점**(스파이크 5.44× 덜 쓰고 0.57 pp 더 잃음).
⚠️ 코퍼스: Table 4 는 Wiki-103 단어수준, 우리 곡선은 Wiki-2 raw.

---

## 3. 대조 과정에서 **논문에 대해** 알아낸 것

| # | 발견 | 쓰임새 |
|---|---|---|
| **1** | **Table 4 의 `T` 축은 `8/10/12/16`** — `2/4/8/16` 으로 **추정될 뻔했다**. 논문은 `T=4`·`T=2` 를 **아예 안 잰다** | 그 추정으로 표를 만들었으면 헤드라인이 통째로 틀렸다 |
| **2** | **본문의 "near-optimal at T=12" 는 GPT-2 엔 거짓** — 33.56 = **+48.2%** (ViT·RoBERTa 에는 참) | 그 문장을 그대로 인용하면 안 된다 |
| **3** | **G.1: *"all learnable parameters in the MBE neuron are fixed as [20]"*** — 항등원은 **학습 자체를 안 한다**. 본문도 *"D … shared across the entire network"* | 🔴 **우리 tied 항등원이 차별점이 아니다.** 339 중 218 이 항등원인데 저쪽 계산으론 전부 공짜 |
| **4** | **Table X 의 `inv` 이 비단조** — N=4(1.1e-3) → N=6(**2.2e-3**) | 적합의 `N` 비단조성이 **우리만의 병리가 아니다**(우리가 훨씬 심할 뿐) |
| **5** | **22.34 vs 22.65 는 `<unk>` 다** — raw test split 은 **sha256 동일**, 단어수준만 6.31% vs 1.03% | **논문은 단어수준 릴리스를 썼다**(연역). raw 로 -103 을 돌리면 그 칸이 안 채워진다 |
| **6** | **저쪽이 자기 저-`T` 실패를 *"wider identity mapping range [0,62]"* 로 진단** | **지수 라우터가 겨냥하는 바로 그 자리 — 서론 동기 문단** |
| **7** | 인쇄된 백분율이 **전부 절대 pp** (Table 1·2·3) | 상대값은 우리가 재계산해야 한다 |

---

## 4. 대조 과정에서 **우리에 대해** 알아낸 것

### 방법이 안정적이라는 증거 — 네 축에서 규칙 출력 불변

| 축 | 섭동 | 결과 |
|---|---|---|
| 캘리브레이션 draw (E12) | disjoint 3벌, 정의역 3–4% 이동 | **19/19 뱅크 동일**, 저장 동일, 스파이크 0.011% |
| **코퍼스** (E3) | 49× 큰 다른 코퍼스, `<unk>` 치환 | **빌드 바이트 동일**, 스파이크 1.001× |
| **적합 epochs** (E8) | 50 → 300 | **저장 바이트 동일** (손실은 48–192× 개선) |
| 하드웨어 | 3개 박스 | **지문 바이트 동일**, ΔPPL 잡음 **0.0064 pp** |

> **"예산을 손튜닝이 아니라 푼다"의 실증이다.** 튜닝한 상수는 이렇게 재현되지 않는다.

### 저장이 모델 **폭**에 무관하다

GPT-2(d=1024, 24층) 339 prims / 13,472 p · ViT-B(768, 12층) 171 / 6,500 ·
**ViT-M(512, 12층) 171 / 6,404**. → **`O(사이트 수)`, 사이트는 깊이 비례.**
*"파라미터를 추가하는 방법 아니냐"* 에 **넓어질수록 상대 오버헤드가 준다**는 답.

### 우리가 지는 축 — 먼저 적을 것

| 축 | 상태 |
|---|---|
| **파라미터** | 3 규약 중 **2 패**. G.1 원문대로면 **5,929 vs 13,472 = 0.44×** |
| **메모리(데이터 이동)** | 최악 dataflow 에서 **패**(연산의 1.8%). tensor-stationary 에선 4.19× 승 |
| **스케일** | 345M / 86M / 64M. NLSpike 는 8B급 4종 |
| **절대 정확도** | 모든 칸에서 비교 불가. ImageNet 은 우리 파이프라인이 **0.8–1.0 pp 낮다**(미규명) |
| **메커니즘 주장** | **C9 삭제** — 우리 전역 MBE 재현이 발산하는데 "전역은 안 된다"와 "우리가 못 맞춘다"를 못 가른다 |

---

## 5. 남은 실험

### 🔴 제출 전 반드시

| # | 항목 | 비용 | 왜 |
|---|---|---|---|
| **E9** | **더 큰 모델 1점** (GPT-2-large / Pythia-1.4B 급) | **~5 h GPU** | **가장 유력한 리젝 사유가 스케일**이고, 이게 부분적으로 막는다. **비용 대비 효과가 가장 큼** |
| **문헌 조사** | 지수/binade 라우팅 선행연구 | 조사 | **신규성이 확인 안 됐다.** MBE 와 NLSpike 만 봤다 |
| **E10 재확인** | NLSpike 원문 대조 | 논문 읽기 | `RELATED_WORK.md` 의 ⓝ 표시가 전부 **2차 인용** |

### 🟡 하면 좋은 것

| # | 항목 | 비용 | 왜 |
|---|---|---|---|
| E12 2단계 | ΔPPL 오차막대 | GPU 1–2점 | 잡음 바닥 0.0064 pp 가 핵심 질문엔 이미 답함 → 우선순위 낮음 |
| CNN 행 | VGG16 / ResNet34 | 배선 확인 + GPU | Table 1 을 통째로 채운다. 어텐션이 없어 범위가 다름 |
| ImageNet 격차 | 0.8–1.0 pp 원인 규명 | 로컬 | 절대값 비교를 열 수 있다 |
| E4 재시도 | `spike_budget` 스윕 | GPU | 현재 S 판정은 "예산 안 준 S" 한 점 |

### ⬜ 다음 사이클

**softmax→matmul 융합** — 남은 최대 에너지 표적(어텐션이 GPT-2 예산의 81.8%,
`av_matmul` 이 최대 사이트 오차). **수치를 크게 바꿔 전면 재평가**를 부르고, **값어치가
모델 의존**임이 밝혀졌다(ViT 에선 어텐션이 68–70%).

---

## 6. 판정 — 이 표로 무엇을 주장할 수 있나

**쓸 수 있는 문장:**

> 구간별 예산을 고정하지 않고 **푸는** training-free 변환 뉴런. 논문 자신의 전역
> `T=16` 과 같은 op 집합에서 **CV·NLU·NLG 세 모달리티 모두** 변환 손실이 낮고,
> **논문이 보고하는 최저 `T` 에서 저쪽 다섯 모델이 전부 무너지는 동안 무손실**이며,
> 그 예산 규칙이 캘리브레이션·코퍼스·적합·하드웨어 네 축에 불변이다.

**쓰면 안 되는 문장:**

* ~~"MBE 를 전방위로 이겼다"~~ — 파라미터·메모리는 진다.
* ~~"전역 multi-basis 는 구조적으로 불가능하다"~~ — **C9, 삭제됨**.
* ~~"메모리를 아낀다"~~ — 논문이 이미 항등원을 고정·공유한다.
* ~~절대 정확도/perplexity 를 나란히 놓는 것~~ — 어느 칸에서도 불가.

---

관련: `PAPER_PLAN.md`(실험 우선순위) · `results/RESULTS_2026-08_cycle.md`(사이클) ·
`results/RESULTS_CV.md`(CV 상세) · `RELATED_WORK.md`(포지셔닝) ·
`PASN_vault/60 - 연구일지/00 - 연구일지 인덱스.md`(실험 기록) ·
`PASN_vault/50 - Reference/MBE 논문 published 설정과 수치.md`(기준선 원문).
