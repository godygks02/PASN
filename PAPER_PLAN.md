# 논문화 실험 계획

작성 2026-08-07, **개정 2026-08-10.** **이 문서가 실험 우선순위의 단일 출처다.**
`PASN_vault/40 - Planning/논문 계획 - 타깃 벤뉴와 실험 로드맵.md`(2026-08-04)를
대체한다 — 그쪽의 벤뉴 분석과 NLSpike 조사는 여전히 유효하니 §7에서 참조하되,
**실험 우선순위는 이 문서를 따른다.**

환경: `C:/Users/cm120/miniconda3/envs/SNN/python.exe` (py3.10, torch 2.11 CPU).
현황 요약과 함정 목록은 `NEXT_EXPERIMENTS.md`.

> **★ 08-10 개정 — 프리즈가 확정됐다** (근거는 §2 E0)
> 1. **논문 빌드 = beta 빌드로 확정.** 헤드라인 ΔPPL = **−0.19%**(마진 **1.76 pp**),
>    지문 **53,888 B / 13,472 p / 339 prims**. **−0.14% 와 49,952 / 12,488 은 이제
>    `--no-pasn-beta` 빌드에만 속하는 과거 수치다** — 인용 금지.
> 2. **사전등록 게이트를 놓쳤다**(0.187% > 0.14%). **밴드를 사후에 넓히지 않는다.**
>    결론은 안 흔들린다(놓친 폭이 마진의 2.7%).
> 3. **C6 회귀 → E13 신설.** 레시피 독립성은 no-beta 빌드에서 잰 것이다.
> 4. **C13(오차막대) 격상 → E12 가 필수가 됐다.** 같은 stride 에서 두 빌드가
>    −0.140 / −0.187 을 낸다. 그 0.047 pp 를 설명 못 하면 E0 의 결정도 방어 못 한다.
> 5. **§4.3 신설 — "어텐션 86.5%" 는 헤드라인 빌드의 예산이 아니다.** 그건 per-bank
>    `T_j` arm 이고, 전역 `T=16` 에서는 **81.8% / 활성함수 8.1%** 다.
> 6. **E5 선행조건 해소** — 프리즈가 끝났으므로 지금 돌릴 수 있다. §4.1 세 규약 숫자를
>    **13,472 로 다시 계산해야 한다.**
>
> **08-09 개정에서 바뀐 것** (근거는 §2 E0)
> 1. **E0 신설 — 빌드 프리즈.** 헤드라인 레코드가 **현재 코드가 만드는 빌드가 아니다.**
>    `beta=0.5` 기본값(08-06)이 모든 GPT-2 레코드(08-02/08-04)보다 **뒤에** 들어왔고,
>    현재 기본 빌드는 **바이트·파라미터 +7.9%**, ΔPPL 은 **한 번도 안 쟀다.**
>    **E0 이전에 GPU 를 쓰면 버리는 시간이다.**
> 2. **E1 축소.** 비용 축 완료(08-07). 남은 건 alloc arm 4점(~16 h GPU)뿐이고,
>    **E0 이후 빌드로** 돌려야 한다.
> 3. **E6 확대.** `op_pareto` 계측 수정(바이트 표)에 **`inv`·`invsqrt` tying 배선**을
>    합친다 — 둘 다 수치를 바꾸므로 **E0 프리즈 안에 들어가야 한다.**
> 4. **E11 신설** — 뱅크 활용도 감사(축퇴 라우터 체계적 탐지 + MoE total/active 표).
> 5. **E12 신설** — 캘리브레이션 배치 분산. 유일하게 식별됐지만 한 번도 안 잰 분산원.

---

## 0. 확정된 것 — 재논의하지 말 것

**헤드라인은 두 축이다: 정확도와 스파이크.**

| 축 | 헤드라인? | 근거 | 배치 |
|---|---|---|---|
| **정확도** | ✅ | Table 1/2/3 대비 세 층위 | 결과 절 |
| **스파이크/에너지** | ✅ | Table XI 6/7, G.4 통화 | 결과 절 |
| 파라미터 | ❌ | 규약에 따라 답이 셋 (§4.1) | **한계 절** + 결과 절의 total/active 표 |

**파라미터를 헤드라인에서 뺀 이유**: 계산은 가능하다(§4.1). 다만 중립 규약(양쪽 다
공유 없음)에서 **PASN이 1.77× 더 쓴다**. 세 규약 중 하나에서만 이기므로, 헤드라인에
두면 논쟁의 중심이 우리가 이기는 축에서 지는 축으로 옮겨간다.

**대신 MoE 계약으로 제시한다.** MoE는 *total* ↑↑ / *active* →↓ 를 두 열로 보고하고
필드가 그 거래를 받아들인다. PASN은 `R`개 뱅크를 저장하고 **입력당 1개만 발화**하므로
같은 구조다:

| | 저장 basis (total) | 입력당 활성 basis (active) |
|---|---|---|
| MBE @ 논문 G.1 | 4 (GELU) / 8 (나머지) | **4 / 8 — 항상 전부** |
| PASN (op_pareto 실측 ⟨N⟩) | Σ_v N_v | **1.14 – 2.29** |

이 표는 새 주장이 아니라 **스파이크 절감의 메커니즘**이다(`T·η·N` 에서 활성 `N`이
줄어드는 것이 곧 에너지). 방어가 아니라 설명으로 배치할 것.
**네트워크 층위 수치는 E11 이 만든다** — 지금 이 표는 고립 op 실측이다.

**MoE 유비가 사주지 않는 것**: 활성이 적어도 메모리는 상주해야 하고 라우팅이 지역성을
깬다. MoE 추론이 memory-bound 인 이유가 그것이다. 이 프레임을 쓰는 순간 **"뱅크 전환
비용은?"** 과 **"뱅크가 실제로 고르게 쓰이나?"** 를 자초하므로 **E7 과 E11 의 우선순위가
내려가지 않고 올라간다.**

### 그 밖의 고정 결정

* **기준선은 논문 published 값만.** 우리 MBE 재현은 내부 ablation (2026-08-01 결정).
* **상대 ΔPPL 만 비교.** 절대 ppl 은 ANN 도 레시피도 달라서 비교 불가.
* **단위 함정**: Table 3 의 `(+0.35)` 는 절대 ppl. 상대는 **+1.57%**.

---

## 1. 주장 → 근거 → 구멍 지도

논문이 할 주장을 먼저 적고, 각각이 무엇으로 서 있고 무엇이 없는지 본다.
**실험은 이 표의 빈칸을 메우기 위해서만 돌린다.**

| # | 주장 | 현재 근거 | 상태 | 남은 것 |
|---|---|---|---|---|
| C1 | 같은 op 집합·같은 `T=16` 에서 **NLG 변환 손실이 논문보다 1.76pp 낫다** | GPT-2-medium ΔPPL **−0.19%** vs Table 3 **+1.57%** — **프리즈 빌드 실측** | ✅ **E0 완료 (08-10)** | — |
| C2 | 같은 뉴런·규칙·코드가 **인코더에서도** 된다 | SST-2 base **+0.12%** vs −1.09%, large **−0.12%** vs −0.25%, 체크포인트 3개 | ✅ | — (MR 은 §6) |
| C3 | **op 층위에서 등정확도 스파이크 우위** | Table XI 7중 6 (2.2–10.3×) · **10/10 op, `1.23×–5.39×`**(`pasn_rule`, tying 반영 재산출 08-10) | ✅ **재산출 완료** | 교차 모델 단서 → E9 로 완화. ⚠️ **`1.61–6.41×` 는 arm 이 섞인 값 — 인용 금지** |
| C4 | **함수근사 층위** 재현·능가 | Table X | ✅ | — |
| C5 | 기여의 실체는 **`N_j` 배분** | 스파이크 2.4–2.55×, 메모리 2.55× (`T_j` 는 1.065×) | ✅ | — |
| C6 | **결론이 평가 레시피에 의존하지 않는다** | stride 3점, ΔPPL 폭 0.197pp, 마진 ≥1.51pp — **단 no-beta 빌드에서 잰 것** | 🔴 **회귀 (08-10)** | **E13** (프리즈 빌드 stride 재측정) |
| C7 | **공유 적합이 깊이에서 안 무너진다** | 24블록 프로파일, 누적 1.39× 포화 | ✅ | `av_matmul` 공시 |
| C8 | ~~논문이 깨지는 저-`T` 영역에서 우리는 안 깨진다~~ → **저-`T` 에서 우아하게 저하되고 T=2 에서 무너진다** | 4점: −0.19 / +0.06 / +2.14 / **+60.97%** (T=16/8/4/2), 스파이크 1.00/3.25/5.44/9.52× | 🟡 **측정 완료, 주장 축소** | **E3** + 리스크 7(Table 4 의 `T` 대응 미확인) — 대조표를 아직 못 만든다 |
| C9 | **전역 multi-basis 는 구조적으로 안 된다**(메커니즘) | 우리 MBE 재현이 전 설정 발산 | 🔴 **재현 실패와 구분 불가** | **E2** |
| C10 | **CV 를 포함한 3모달리티** | 없음 | 🔴 | **E8** |
| C11 | **라우팅이 축퇴하지 않는다**(MoE 부하 균형) | `1/x` 축퇴를 **우연히** 발견해 고침 | 🔴 **체계적 확인 없음** | **E11** |
| C12 | 메모리 축 | 논문 미보고. 내부적으로 전역 MBE 와 사실상 동률(1.08×). op 표는 tying 후 바이트가 1.7–2.7× 개선되나 **`pasn_rule` 에서는 여전히 10/10 패**(0.11–0.93×) | 🟡 | E5·E7. ⚠️ **"메모리 축 첫 승"(`layernorm` 1.30×)은 `pasn`(라우팅만) arm 에서만 성립** — arm 없이 쓰지 말 것 |
| C13 | 헤드라인에 **오차막대**가 있다 | 체크포인트 분산(NLU) + **캘리브레이션 빌드 층위(E12 1단계)**: 저장·예산 **완전 불변**, 스파이크 spread **0.0112%** | 🟡 **1단계 완료 (08-10)** | **E12 2단계** — 빌드 불변성은 ΔPPL 불변성이 아니다(적합 정의역은 3–4% 움직인다). **−0.19% 에 붙일 숫자는 아직 없다.** GPU 1–2점, **E13 뒤** |

### 논문 표 커버리지 — 빈칸 지도

| 논문 표 | 내용 | 상태 | 담당 |
|---|---|---|---|
| Table 3 Wiki-2 | NLG +1.57% | ✅ **−0.19% (프리즈 빌드)** | 완료 |
| **Table 3 Wiki-103** | NLG **+3.36%** | ❌ | **E3** |
| **Table 4** | **timestep 견고성** | 🟡 비용 축만 | **E1** |
| Table 2 NLU | RoBERTa 4종 | SST-2 base/large ✅ · MR 판정불가 · SST-5/Subj ❌ | 체크포인트 부재 — §6 |
| Table 1 CV | ViT-B/16 −0.44%, M/16 −0.64% | ❌ | **E8** |
| Table X | 함수근사 MSE vs N | ✅ 재현·능가 | 완료 |
| Table XI | op 별 발화율 | ✅ 6/7, 2.2–10.3× | E9 로 완화 |
| G.1 하이퍼파라미터 | N=4/8, T=16 | ✅ 매칭 (N 은 우리가 적음) | 완료 |
| G.4 에너지 모델 | E_AC=0.9pJ | ✅ 상수 일치 | 메모리 접근은 E7 |

---

## 2. 실험 목록 — 의존 순서대로

각 항목의 **완료 조건**을 만족하면 닫고, 아니면 열어둔다. "돌렸다"는 완료가 아니다.

### ⛔ 전체를 지배하는 제약 — 프리즈 먼저

**수치를 바꾸는 변경은 전부 재평가를 강제한다.** 지금 열려 있는 수치 변경이 셋이다:
`beta=0.5`(이미 기본값이 됐는데 재평가 안 됨), `inv`/`invsqrt` tying(E6),
softmax→matmul 융합(§3). GPU 는 Stage 2 한 점이 **13.3시간**이다.

> **순서를 틀리면 16시간을 두 번 낸다.** E1(4점 × 4h)을 먼저 돌리고 E6 을 넣으면
> 그 16시간이 전부 무효가 된다. **수치 변경을 한 번에 모아 닫고(E0), 그 빌드를
> 논문 빌드로 고정한 뒤에만 GPU 스윕을 시작한다.**

```
        [E6 배선] ┐
       [E11 감사] ┼→ ★E0 프리즈+헤드라인 재검증 ─→ E1(GPU) ─→ E3(GPU)
   [융합? 보류결정]┘                              └→ E12(분산)
                                                  └→ E8/E9(GPU, 조달 후)
   병렬(런 불필요): E2 · E5 · E7 · E10
```

---

### ★ E0 — 빌드 프리즈와 헤드라인 재검증 · ✅ **완료 2026-08-10 · 프리즈 = beta 빌드**

> **결과** → `PASN_vault/60 - 연구일지/E0 - 빌드 프리즈와 헤드라인 재검증.md`,
> `results/e0_beta_attrib.json`(귀속) + **`results/freeze_e1.json` 의
> `freeze-t16-unif`**(프리즈 빌드 헤드라인). 로컬 CPU 37분 + GPU 3.3 h.
>
> ✅ **귀속 완료 — 드리프트는 100% `pasn_beta`.** `--no-pasn-beta` 가
> **49,952 B / 12,488 p / 339 prims** 로 헤드라인과 **정확히 일치**. 기록되지 않은
> 변경은 **없다** → E1 §6 을 닫는다.
> ✅ **스키마를 구조로 고쳤다** — 손으로 나열하던 것을 그만두고 빌드된
> `ConvertConfig` **37 필드 전체를 `convert_cfg` 로 직렬화**. flat 키는 `p04_report`
> 호환 위해 유지. `--no-pasn-beta` 신설(귀속 지렛대 + 헤드라인 빌드 재현 수단).
> ✅ **재평가 완료 — 프리즈 빌드의 ΔPPL = −0.1873%** (ANN 21.7058 → SNN 21.6652,
> stride 1024). **53,888 B / 13,472 p / 339 prims / 2.4014e9 스파이크.**
> ✅ **결정: beta 빌드를 논문 빌드로 확정.** 마진 1.71 → **1.76 pp**. 근거는 op 표
> (`1/x` 0.85×→5.39×, softmax 1.88×→6.41×, **9/10 → 10/10**)가 저장 +7.9% 보다 값짐.
> ✅ **네트워크 층위에서 beta 는 스파이크를 안 쓴다** — 같은 박스 no-beta 대비
> 총 스파이크 **0.999×**, op 구성 동일. **대가는 저장 하나뿐**(함정 15 와 정합:
> beta 의 값어치는 고정 운영점이 아니라 등정확도 프론티어에서 나온다).
> ⚠️ **사전등록 게이트를 놓쳤다** — 조건 (b) 는 `|ΔPPL| ≤ 0.14%` 인데 0.187% 다.
> **밴드를 사후에 넓히지 않는다.** 그 밴드는 no-beta 빌드에서 측정된 것이라 애초에
> 이 빌드의 것이 아니었지만, 게이트는 사전 선언됐고 충족되지 않았다. 놓친 폭
> (0.047 pp)은 마진의 **2.7%** 라 결론은 안 흔들린다.
> 🔴 **대가 — C6 회귀.** 레시피 독립성은 no-beta 빌드의 stride 3점에서 세운 주장이고,
> 프리즈 빌드는 **stride 1024 한 점**뿐이다 → **E13 신설.**
> 📌 **부수 발견 — "어텐션 86.5%" 는 헤드라인 빌드의 예산이 아니다** → §4.3.

### (원문) E0 설계 근거

**왜 — 헤드라인 레코드가 현재 코드의 산물이 아니다.**

| 빌드 | 날짜 | bytes | params | spikes | ΔPPL |
|---|---|---:|---:|---:|---:|
| `stride_sens` (헤드라인) | 08-04 | 49,952 | 12,488 | 2.404e9 | **−0.1397%** |
| `gpt2_stage2_fixed` | 08-02 | 49,952 | 12,488 | 2.389e9 | (동일 빌드) |
| **현재 기본값** (`timestep_sens` t16) | 08-07 | **53,888** | **13,472** | 2.377e9 | **없음** |

`ConvertConfig.pasn_beta = {"inv": 0.5}` 가 **08-06** 에 기본값이 됐다. 모든 헤드라인
레코드는 그 **이전**이다. 차이는 **+7.9% 바이트/파라미터**이고, `inv` 는 softmax 경로에
있으므로 **스파이크 예산의 37.9% 를 차지하는 op 의 수치가 바뀐다.**

**더 나쁜 것 — 소급 감사가 불가능하다.** GPT-2 레코드 스키마에 **`pasn_beta` 가 없다**
(`pasn_id_tied`, `pasn_e_min`, `pasn_readout_order` 등은 있는데 beta 만 빠졌다).
어떤 레코드도 자기가 어느 beta 로 만들어졌는지 말하지 못한다. E1 §6 이 "베이스라인이
움직였다 — 미해결" 로 남긴 것이 이것이고, **가설이 아니라 확인된 사실이다.**

**리뷰어 관점에서 이건 재현성 결함이다.** "논문 수치가 저장소 기본값으로 재현되지
않는다" 는 rebuttal 로 막을 수 없다.

**할 일.**
1. **레코드 스키마 보강** — `pasn_beta` 를 포함해 **빌드를 바꿀 수 있는 모든 기본값**을
   레코드에 직렬화한다. 함정 9(받아들여진 손잡이 ≠ 적용된 손잡이)의 계측판이다.
   덤으로 앞으로 "어느 빌드였나" 를 다시 묻지 않게 된다.
2. **E6 배선을 여기서 끝낸다**(아래) — tying 도 수치를 바꾸므로 같은 프리즈에 넣는다.
3. **융합(§3)은 프리즈에서 뺀다** — 규모가 커서 이번 사이클에 못 넣는다. 명시적 결정.
4. **헤드라인 1점 재평가**: `--stride 1024`, gpt2-medium, `T=16`, 현재 기본값.
5. **바이트 증가의 귀속 확인**: `beta` 만 끄고 빌드해 49,952 로 돌아오는지 diff.
   돌아오면 원인 확정, 안 돌아오면 **다른 미기록 변경이 있다는 뜻이라 더 급하다.**

**비용.** 배선·diff 는 로컬 CPU(빌드 1회 ≈ 900 s). 재평가 **1점 ≈ 2.4 h GPU**
(`stride_sens` 실측: build 1830 s + ANN 22 s + SNN 6641 s). ⚠️ **"13.3 h" 는 stride
3점 스윕 전체**(1830 + 22+45+90 + 6641+13206+26031 = 47,864 s)이지 1점이 아니다 —
`NEXT_EXPERIMENTS.md` §E9 와 구 계획의 인용이 이걸 1점으로 오독했다.

**완료 조건.** (a) 현재 기본값 빌드의 ΔPPL 이 나오고, (b) 그 값이 `|ΔPPL| ≤ 0.14%`
band 안에 있으며, (c) 레코드가 자기 beta·tying 설정을 담고, (d) `PASN_method.md` 와
연구일지의 헤드라인 수치가 그 값으로 갱신된다. **band 를 벗어나면 그게 결과다** —
어느 쪽이 논문 빌드인지 **의도적으로** 결정하고 기록한다.

> **판정(2026-08-10): (a) ✅ −0.1873% · (b) ❌ 밴드 밖 · (c) ✅ 37 필드 직렬화 ·
> (d) ✅ 갱신 완료.** (b) 가 발동했으므로 계획서가 미리 적어둔 대로 **의도적 결정**을
> 했다 — **beta 빌드가 논문 빌드다**(근거는 위 블록). **밴드는 다시 그리지 않는다.**

```bash
python experiments/gpt2_wikitext.py --backend mbe_pasn --convert-ops all \
    --model gpt2-medium --epochs 300 --stride 1024 --pasn-t-fixed 16 \
    --pasn-id-target relative --pasn-id-target-rel 1e-2 \
    --json results/freeze_e1.json --tag freeze-t16-unif
```

---

### E13 — 프리즈 빌드의 stride 민감도 · 🔴 **신설 2026-08-10 · C6 을 되찾는 유일한 길**

**왜.** C6("결론이 평가 레시피에 의존하지 않는다")은 헤드라인 뒤에 남아 있던 **유일한
열린 가정을 닫은** 주장이었는데, **no-beta 빌드에서** 잰 것이다. E0 이 프리즈를 beta
빌드로 확정한 순간 **그 근거가 프리즈 빌드의 것이 아니게 됐다.** 현재 프리즈 빌드는
stride 1024 한 점뿐이다.

**위험도는 낮고, 그게 이 항목을 뒤로 미룰 수 있는 이유다.** 두 빌드가 같은 stride 에서
0.0476 pp 차이인데 레시피 유발 폭은 0.197 pp 다. 이동이 대략 균일하면 sign-flip
지점이던 stride 256 이 +0.058% → **+0.011%** 로 여전히 밴드 안이다. **단 그건 추정이지
측정이 아니다** — 그리고 이 프로젝트는 "추정으로 닫았다가 나중에 틀린" 전례가 있다
(함정 15 의 `1/x` 가 두 번 잘못 닫혔다).

**선택지.** 빌드 1회를 재사용하고 `--stride` 가 여러 값을 받으므로 평가만 추가된다.

| 안 | 내용 | GPU | 결과 |
|---|---|---:|---|
| **A** | stride 512 한 점 | ~5 h | 2점 확인 + 256 은 no-beta 에서 이전 논증 |
| B | 512 + 256 | ~12 h | C6 완전 회복 |
| C | 안 함 | 0 | "stride 1024 에서 무손실" 로 좁히고 C6 을 헤드라인에서 뺌 |

**→ 8/30 워크숍까지 A, ICML 본 제출 전 B.** 그 전까지 C6 문장은
**"stride 1024 에서 측정상 무손실"** 로만 쓴다.

⚠️ **E1 보다 뒤다.** E1 은 2.4 h 에 *"저쪽은 깨지고 우리는 안 깨진다"* 를 사고, E13 은
5 h 에 *이미 서 있는 주장의 근거를 빌드 하나만큼 옮긴다.* 같은 GPU 시간이면 E1 이 먼저다.

**완료 조건.** 프리즈 빌드에서 stride ≥2점의 ΔPPL 이 나오고, 그 폭이 논문 마진
(1.76 pp) 대비 몇 %인지 말할 수 있다.

---

### E6 — 동차 프리미티브 tying + `op_pareto` 바이트 정정 · ✅ **완료 2026-08-09**

> **결과** → `PASN_vault/60 - 연구일지/E6 - 동차 프리미티브 tying과 op 표 정정.md`,
> `results/op_pareto_tied.json`(세 op 의 해당 행을 대체). 테스트 46개 통과.
>
> ❌ **`inv` tying 불가 — 아래 원문의 전제가 틀렸다.** `beta` 가 라우팅 키를 옮기는데
> 동차성은 그 키에서 성립해야 한다. `1/(t+0.5)` 는 비동차라 빌더가 raise(residual
> 4.21e-3 vs tol 1e-3). **분할 ≠ 동차성**이고 둘은 `inv` 에서 상호배타다. `beta` 가
> 훨씬 값지므로(MSE 25×, 스파이크 5.9×) **untied 확정**.
> ⚠️ **`invsqrt` 는 되지만 안 가져온다** — 저장 2× 를 스파이크 1.19×·오차 1.11× 로
> 사는데 E11 이 rsqrt 를 원소 **0.0%** 로 쟀다. `pasn_invsqrt_tied` 기본 **False**
> (비트 동일 확인) → **프리즈에 수치 변경 없음.**
> ✅ **본체 = op 표.** `TIED_PRIMS={"identity"}`. **바이트가 MBE 대비 2–4× 개선**,
> `layernorm`@3e-2 는 **0.35× 패 → 1.30× 승**(메모리 축 첫 승). `attention` 은 MBE 가
> 못 가는 정확도(1e-2, 3e-3)에 도달.
> ✅ **통제**: `mbe` arm 45행 비트 동일 → 변화는 tying 귀속, 환경 드리프트 아님.
> ✅ **후속 완료 2026-08-10** → 일지 §9, `experiments/op_pareto_merge.py`,
> `results/op_pareto_merged.json` + `_wins.md` + `op_pareto_merged.md`.
> **10/10 승은 살아남고, 논문에 쓸 범위는 `1.23×–5.39×`**(`pasn_rule`, 승자 중앙값
> 3.27×). op 별 이동: `attention` 1.45→**1.70×**, `layernorm` 2.43→1.55×,
> `fp_multiply` 1.61→1.23×.
> ⚠️⚠️ **재산출의 진짜 수확 — `1.61–6.41×` 는 op 마다 최선 arm 을 고른 범위였다.**
> 계측 버그가 아니다. 실험 12 §4 표는 열 이름이 「최선 arm」이고 **행마다 arm 이
> 적혀 있다**(softmax → `pasn`, inv → `pasn_rule`, …). 문제는 **그걸 한 줄로 접은 요약
> 문장**이고, C3 로 옮겨 온 게 그쪽이다. **논문에서는 arm 쇼핑이라 못 쓴다** — 리뷰어는
> "the method" 를 하나의 설정으로 읽는다. `6.41×` 는 `pasn`(ablation)의 softmax,
> 같은 op 이 `pasn_rule` 에서는 **4.23×** 다 → §5 에 금지 항목 추가.
> ⚠️ **"메모리 축 첫 승" 도 `pasn` arm 에서만 성립**(`layernorm`@3e-2 가 `pasn` 에서
> 0.35→**1.30×**, `pasn_rule` 에서는 0.35→0.57× **패**). **바이트는 `pasn_rule` 에서
> 10/10 패** — §0 의 "메모리는 헤드라인이 아니다" 와 일치.
> ✅ **`attention` 은 수준이 아니라 모양이 바뀌었다** — 배포 운영점 쪽이 크게 좋아진다
> (2.56→**4.90×**, 바이트 0.49→**0.99×** 동률 근접), 느슨한 쪽은 악화(1.02→0.19×).
> **여섯 행을 다 실을 것.**
> ⚠️ **`fp_multiply` 공시**: 1.23×, 매칭 **2/3**, 가장 정확한 MBE 점(4.88e-4)에는
> **구·신 어느 PASN 빌드도 도달 못 한다** — tying 탓이 아니라 원래 있던 천장이다.
> ✅ **통제를 스크립트가 강제한다** — 병합 전에 `mbe` arm 45빌드 × 9필드 비트 동일을
> 확인하고, 아니면 **병합을 거부**한다(`--force` 로만 우회). 함정 13 의 계측판.

### (원문) E6 설계 근거 — ⚠️ `inv` 관련 서술은 위에서 반증됨

**왜 두 개를 합치나.** 같은 뿌리다 — `tied=True` 가 `_routed_identity` 에만 닿는다.
결과가 두 곳에서 다르게 샌다:

* **변환 경로**: `invsqrt`(`convert.py:676`)·`inv`(`convert.py:596`) 가 tying 없이
  빌드된다. 둘 다 양의 동차(`k=−1/2`, `k=−1`)라 라우터 지수가 그대로 인수분해된다.
  실험 4 §4 가 셋 다 `r=1e-3` 에서 **208 params → 13**, 정확도 불변으로 확인했다.
* **op 표**: `op_pareto.py` 가 `tied=True` 를 안 넘겨서 `fp_multiply`(0.06×)·
  `layernorm`(0.13×)·`attention`(0.18×) 의 바이트 열이 **우리 메모리 비용을 3–16×
  부풀려 보고한다.** 논문에 갈 표다.

**만료된 메모에 속지 말 것.** 실험 4 §5 의 *"`MBE_inv` 는 여전히 라우팅 불가"* 는
**08-06 에 참이 아니게 됐다** — `beta=0.5` 가 뱅크를 나눠 놨으니 이제 tie 할 뱅크가
있다. `invsqrt` 는 애초에 그 논리에 해당하지도 않았다(`[0.5,2)` = binade 2개).

**주의.** 프리미티브 단위 게이팅이 필요하다 — `attention`/`softmax` 는 `exp2` 도 짓는데
비동차라 `build_mbe_pasn` 이 raise 한다(`mbe_pasn.py:513`). `maker()` 에서
`{identity, inv, invsqrt}` 로 게이트할 것. τ 공유(§3)와 **더하지 말 것** — `TiedBank` 가
참조를 들고 있어 tied 뱅크는 이미 τ 를 공유한다.

**비용.** 배선 + op 3개 재측정 ~18분. **단 기존 `pasn_rule` 레코드 1개를 먼저
재빌드해 diff 할 것** — 재현 안 되면 4 arm × 3 op 전부(~82분).

**완료 조건.** 변환 경로에서 세 프리미티브가 tied 로 빌드되고, op 표의 바이트 열이
갱신되며, **E0 재평가가 이 빌드로 돌아간다**(별도 GPU 런 아님).

---

### E11 — 뱅크 활용도 감사 + MoE total/active 표 · ✅ **완료 2026-08-09**

> **결과** → `PASN_vault/60 - 연구일지/E11 - 뱅크 활용도 감사.md`,
> `results/bank_usage_audit.json` (339 사이트 전수), `experiments/bank_usage_audit.py`.
> 로컬 CPU 23.1분.
>
> ✅ **`dead`(도달 뱅크 1개) 0개** — `1/x` 형 축퇴는 네트워크 어디에도 없다.
> `beta=0.5` 가 국소 땜질이 아니었다는 확인.
> ✅ **`invsqrt` 열린 가정 해소** — rsqrt 관측 binade **1.90**, 4뱅크 중 2개 도달.
> 축퇴 아님(추론이 옳았다), 원소 비중 0.0%.
> ✅ **MoE 표가 네트워크 층위로** — §0 의 고립 op ⟨N⟩ 을 실모델 값으로 교체:
> **저장 1832 basis vs 입력당 활성 1.57**(활성함수 33 저장 / **1.01** 발화).
> 🔴 **대신 다른 종류의 축퇴를 찾았다 — `av_matmul.idn` 24개 전부.** 정의역이 좁아서가
> 아니라 **질량이 near-zero 뱅크에 쏠려서**다(top-1 0.973–**0.992**, `n_eff` 1.06–1.17,
> 뱅크 8개). 입력이 softmax 출력이라 평균 `1/S`=0.0039 인데 near-zero 상한이
> `2^-6`=0.0156 — **정의상** 삼켜진다. **깊이에 따라 악화**(L0 → **L21** 최악).
> **예산·오차·점유 세 측정이 같은 프리미티브의 같은 레이어를 짚는다.**
> → **E11b 로 이어진다(아래).**

### E11b — `av_matmul` 항등원 라우터 심화 · ✅ **완료 2026-08-09 · 결론: −6 유지**

> **결과** → `PASN_vault/60 - 연구일지/E11b - 항등원 라우터 심화.md`,
> `results/id_router_depth_t16.json`(배포 설정) + `results/id_router_depth.json`(동적 T).
> 로컬 CPU 40셀 24분.
>
> ✅ **필요조건 성립** — `n_eff` 1.19 → **4.69**. 질량이 실제로 갈린다.
> ✅ **정확도 천장도 실재** — −6 은 nrmse **5.78e-3** 이 바닥, 깊은 쪽은 3.41e-3.
> ⚠️ **그런데 등정확도 프론티어가 nrmse ≈ 6.6e-2 에서 교차하고, 배포 운영점
> (`target 1e-2`, `T=16`)이 내는 값이 정확히 6.59e-2** — **−6 이 2.4× 이기는 쪽**
> (66.5 vs 159.1 스파이크). 네트워크는 이미 무손실이라 더 정확해봐야 살 게 없고,
> 이 사이트는 라우팅 원소의 26.1%·matmul 은 예산의 48.6% 다.
> **→ `pasn_id_e_min = −6` 유지. 프리즈 대상은 현재 기본값 그대로.**
> ✅ **E1 방향으로 더 안전해진다** — 저 T 는 운영점을 −6 이 더 크게 이기는 쪽으로 민다.
> 📌 **뒤집을 조건**(일지 §5): 이 사이트에 nrmse 5e-2–1e-2 가 필요해지거나, 6e-3 보다
> 정확해야 하거나, ctx 1024 에서 집중이 더 심해져 교차점이 움직이는 경우.

### (원문) E11b 설계 근거

**왜.** E11 이 찾은 것을 고칠지 말지 정해야 하고, **수치를 바꾸므로 E0 프리즈 안에서
결정해야 한다.** 프리즈 뒤에 넣으면 GPU 세션을 다시 낸다.

**후보 손잡이는 `beta` 가 아니라 `pasn_id_e_min`**(현재 −6). 바닥을 낮추면 near-zero
뱅크가 삼킨 질량이 여러 binade 로 갈린다. `softmax/idn`(top-1 0.904)도 같은
`_routed_identity` 를 지나므로 **한 번에 같이 움직인다.**

**⚠️ 판정 설계 — 함정 15 를 세 번째로 밟지 말 것.**
* **op 나머지를 고정하고 재지 말 것.** 물어야 할 것은 *"등정확도에서 `av_matmul` op 이
  얼마인가"* 다. `op_pareto --ops attention` 으로 예산을 풀고 잰다.
* **실험 2 의 "`e_min`↓ 무효" 를 근거로 기각하지 말 것** — 그건 활성함수에서 잰
  것이고 어텐션 확률은 분포가 다르다. **동시에 그게 여기서 먹힌다는 근거도 아니다.**
* **저장 대가를 같이 보고할 것.** `beta=0.5` 도 그 프리미티브에서 3.7× 저장을 냈다.

**완료 조건.** `pasn_id_e_min ∈ {−6, −8, −10}` 정도에서 (a) 점유가 실제로 갈리는지
(`bank_usage_audit.py` 재실행, 싸다), (b) 등정확도에서 op 스파이크·바이트가 어떻게
움직이는지. **개선이 없으면 그것도 결과다** — 기각하고 프리즈를 −6 으로 확정한다.

### (원문) E11 설계 근거 — 왜 이 감사가 필요했나

**왜 — `1/x` 축퇴는 우연히 발견했다.** 7뱅크 중 1개만 도달 가능하고 20000/20000 입력이
거기 들어가던 것을, **Table XI 에서 지는 행을 쫓다가** 찾았다. 그 전에 두 번(P0.2,
`inv_router_fix` 1차) **잘못 닫혔다**(함정 15). 지금 **다른 사이트에 같은 축퇴가 없다는
근거가 없다.** GPT-2-medium 은 사이트가 339개다.

**MoE 프레임이 이걸 강제한다.** §0 에서 total/active 계약을 채택하는 순간 리뷰어의 다음
질문은 MoE 문헌의 표준 질문이다 — **"expert 가 고르게 쓰이나, 죽은 expert 는 없나."**
답을 준비 못 하면 프레임 자체가 부채가 된다.

**할 일.** 캘리브레이션 입력으로 **사이트별 뱅크 점유 히스토그램**을 뽑는다(빌드 전용,
평가 불필요). 산출물 셋:
1. **축퇴 탐지** — 도달 뱅크 1개이거나 점유율이 한 뱅크에 >99% 몰린 사이트 목록.
   나오면 `beta` 후보를 그 프리미티브의 정의역 하한으로 잡는다(`inv` 에서 한 것 그대로).
2. **MoE total/active 표 — 네트워크 층위.** §0 의 표가 지금 고립 op 실측인데,
   이걸 GPT-2 전체에서 `Σ_v N_v` (저장) vs 입력당 기대 활성 `N` 으로 바꾼다.
   **논문의 메커니즘 그림 1장이 여기서 나온다.**
3. **부하 분포 그림** — expert 활용도 히스토그램. MoE 논문 관례 그대로.

**비용.** 로컬 CPU, 빌드 전용. `layer_error_profile.py`(21분) 와 같은 형태의 순회.

**완료 조건.** 339 사이트 전부에 점유 분포가 있고, 축퇴 사이트가 **0개임을 보이거나
목록이 나온다.** 나오면 프리즈 전에 고친다(그래서 E0 보다 먼저다).

---

### E1 — Table 4 timestep 견고성 · ✅ **두 축 완료 2026-08-10 · 주장은 축소된다**

> **정확도 축 최종** → 일지 §9, `results/freeze_e1.json`(4 레코드).
>
> | T | ΔPPL | 스파이크 | 에너지 |
> |---:|---:|---:|---:|
> | 16 (unif) | **−0.187%** | 1.00× | 1.00× |
> | 8 (alloc) | **+0.062%** | **3.25×** | 1.60× |
> | 4 (alloc) | **+2.137%** | 5.44× | 2.04× |
> | 2 (alloc) | **+60.97%** | 9.52× | 2.41× |
>
> ✅ **발산하지 않는다**(34.94 = ANN 의 1.61×, 논문의 41072 같은 값이 아니다).
> 🔴 **그러나 T=2 는 쓸 수 없다 — 완료 조건이 예고한 "발산하면 그것도 결과다" 쪽이다.**
> **무릎은 T=4–2 사이이고 그 거래가 명백히 나쁘다**: 손실 **28×** 를 에너지 **1.18×**
> 로 산다. 비-스파이크 오버헤드가 T 무관 상수라 저 T 에서 스파이크를 더 깎아도 에너지가
> 안 따라온다(§4 와 정합).
> ✅ **운영점 = T=8** — 스파이크 **3.25×** 를 **+0.062%** 에 산다(stride 밴드 0.197pp 안
> = 측정상 무손실). T=4 도 쓸 수 있다. **T=2 는 못 쓴다.**
> ⚠️ **서사를 축소해야 한다.** ~~"저쪽은 깨지고 우리는 안 깨진다"~~ →
> **"T≥4 에서 우아하게 저하되고 T=2 에서 무너진다."**
> ⚠️⚠️ **논문 Table 4 와 아직 나란히 못 놓는다** — (a) 리스크 7: 기준선 문서에 `T`
> 대응이 없다, (b) 코퍼스가 Wiki-103 vs Wiki-2(E3). **그리고 같은 코퍼스에서는 T=4 에
> 이미 진다**(논문 T=16 **+1.57%** vs 우리 T=4 **+2.14%**) — 승리가 아니라 **교환점**:
> *"스파이크 5.44× 덜 쓰고 0.57 pp 더 잃는다."* **Wiki-2 에서 앞서려면 T≥8.**
> ⚠️ T=16 만 unif arm, T=4/2 는 torch 2.12 박스(ΔPPL 만 읽으므로 안전 — 게이트에서
> ANN 21.7058 재현). 바이트는 네 점 전부 53,888(저장은 `T` 에 불변).

### (원문) E1 설계 근거 · 🟡 비용 축 완료 + 정확도 축 2/4

> **비용 축 결과** → `PASN_vault/60 - 연구일지/E1 - 타임스텝 예산 스윕 (비용 축).md`.
> `--n-steps` 배선 완료(규칙의 `T_GRID` 를 자른다; T=16 에서 no-op, 비트 동일 확인).
>
> **정확도 축 진행 (2026-08-09, `results/freeze_e1.json`)**
>
> | T | arm | ΔPPL | 상태 |
> |---:|---|---:|---|
> | 16 | unif (= E0 프리즈 런) | **−0.1873%** | ✅ |
> | 8 | alloc | **+0.0615%** | ✅ |
> | 4 | alloc | — | ❌ 박스 종료로 유실 |
> | 2 | alloc | — | ❌ 미착수 |
>
> **T=8 이 이미 핵심이다** — 논문 Table 4 는 같은 자리에서 22.65 → **41072** 로
> 발산하는데 우리는 **+0.062%**, stride 스윕이 낸 밴드 안이다.
> ⚠️ **T=16 점만 arm 이 다르다**(unif). 곡선 제목에 표기하거나 3.2 h 를 더 낸다 —
> `T_j` 가 1.06–1.12× 뿐이므로 **표기 쪽을 택한다.**
> ⚠️ **코퍼스가 다르다** — Table 4 는 Wiki-103, 우리는 Wiki-2 (→ E3).

**왜.** 논문이 **파국적으로 지는 유일한 표**다. Wiki-103 에서 T 를 줄이면
`22.65 → 41072 → 11992 → 33.56 → 23.41`. 여기서 PASN 이 우아하게 저하되면 서사가
*"조금 낫다"* 에서 ***"저쪽은 깨지고 우리는 안 깨진다"*** 로 바뀐다.
**0.14% vs 1.57% 보다 훨씬 강한 그림이다.**

**비용 축이 답한 것과 뒤집은 것.**
* ❌ **가설 반증**: "T 가 부족하면 `T_j` 배분이 값어치를 갖는다" 는 **틀렸다.**
  unif/alloc = **1.066 / 1.122 / 1.058 / 1.000** (T=16/8/4/2), 단조도 아니다.
  alloc 의 활성함수 평균 `T` 가 16/8/4 내내 **3 고정** — 규칙이 이미 인색해서 배분할
  여유가 없었다. → **논문에서 `T_j` 를 기둥으로 세우지 말 것. "규칙이 자동으로 옳은
  답을 낸다" 는 검증으로 쓸 것.**
* ✅ **상금**: T=16→2 에서 **스파이크 9.52×**. ⚠️ **에너지는 2.41× 뿐** — 라우터 연산이
  16→2 에 **1.00×**(T 무관 상수)라 저 T 에서 오버헤드가 지배한다.
  **스파이크 9.5× 를 에너지로 인용 금지.**
* ⚠️ **op 구성이 뒤집힌다** — matmul 45.4%→17.0%, T=2 최대는 softmax 40.2%.
  **"어텐션 86.5%" 는 T=16 이야기**이므로 융합의 값어치가 운영점 의존이다.

**남은 것 = `T ∈ {4,2}` 두 점, ~2.4 h.** (T=8 은 완료, T=16 은 E0 프리즈 런이 겸함.)
**arm 두 개를 다 평가할 이유가 없다** — `T_j` 가 1.06–1.12× 뿐이니 **alloc arm 만**
(우리 방법 그대로, 더 쌈). 실행 브리핑은 `experiments/E1_RESUME.md` 에 자립형으로 있다.

**⚠️ 선행 조건.**
1. ~~**E0 프리즈 이후**에 돌린다~~ ✅ **충족.** 프리즈 확정(08-10)됐고, 완료된 두 점이
   이미 프리즈 빌드다.
2. **논문 Table 4 는 Wiki-103 이다.** 같은 축에 놓으려면 **E3 가 선행**하거나,
   최소한 *"어느 코퍼스에서 잰 T 곡선인지"* 를 표 제목에 박아야 한다.

**비용 — 실측 기준 남은 것은 ~2.4 h.** 측정된 T=16 은 build 5262 s + eval 6732 s =
**3.3 h**(예상 2.4 h 보다 비쌌다), T=8 은 **1.9 h**. 평가가 `T` 에 거의 비례하므로
T=4 ≈ 1.2 h, T=2 ≈ 0.8 h. 실행 브리핑은 `experiments/E1_RESUME.md` 에 자립형으로 있다.

> ⚠️ **스파이크 축을 GPU 레코드로 만들지 말 것**(함정 13). 남은 두 점은 박스가 달라도
> **안전하다** — 필요한 건 ΔPPL 뿐이고 그건 박스 간 재현된다(stride 스윕 + 새 박스가
> ANN 21.7058 을 정확히 재현). **T 곡선의 스파이크 축은 이미
> `results/timestep_sens_cost.json`(로컬 CPU 8빌드, 9.52×)에 완결돼 있다.**
> 바이트도 환경 무관(두 박스 53,888 동일)이라 섞어도 된다.

**완료 조건.** T ∈ {2,4,8,16} 에서 ΔPPL 을 논문 Table 4 와
같은 축에 놓고 **PASN 이 발산하지 않음**을 보인다. 발산하면 그것도 결과다 — 어디서
무너지는지 적는다.

---

### E3 — Table 3 Wiki-103 행 · E1 의 축 정합에 필요

**왜.** 표의 빈칸이고, 논문이 Wiki-2(+1.57%)보다 **크게 지는 쪽**(+3.36%)이며,
**Table 4(=E1)가 사는 코퍼스**다.

**⚠️ 먼저 확인할 것.** WikiText-2 와 103 은 **test split 이 같은 문서**다. 논문이
22.34 vs 22.65 로 다르게 적은 이유(전처리? word-level vs raw? fine-tune?)를 규명하지
않으면 우리 행이 무엇과 비교되는지 모른다. 캘리브레이션(train split)은 확실히 다르다.
**런 불필요, 논문 읽기 — 그리고 이건 E1 보다 먼저 할 수 있다.**

**막는 것.** `WIKITEXT_CONFIG` 가 모듈 상수로 하드코딩(`gpt2_wikitext.py:35`).
`--dataset` 플래그 추가(小).

**비용.** E1 과 같은 런에 흡수 가능(캘리브레이션만 다르고 파이프라인 동일).

**완료 조건.** 논문의 두 행이 왜 다른지 답을 적고, 그 위에서 우리 ΔPPL 을 낸다.

---

### E2 — 베이스라인 재현 간극 ★ 신뢰도 1순위 · 런 불필요

**왜.** 메커니즘 주장 C9("전역 multi-basis 는 GELU near-zero 에 구조적 오차 바닥이
있다")의 실증 근거가 전부 우리 MBE 재현인데, 그게 24레이어에서 **모든 용량 설정에서
발산**한다(ppl 611–589287, ANN 21.71). **"전역은 안 된다"와 "구현을 잘못했다"가
구분되지 않는다.** 리뷰어가 가장 먼저 찌를 자리다.

**비용.** 로컬 CPU + 분석. 런보다 진단이다.

**완료 조건.** 둘 중 하나. (a) 발산 원인을 규명하고 문서화한다. (b) 논문 수치를
재현한다. **어느 쪽도 안 되면 내부 ablation 을 논문에서 전부 빼고 published 표 대비
비교만 남긴다** — 그 경우 §진단 절을 다시 써야 하므로 **늦게 알면 안 된다.**

---

### E5 — 파라미터 모델 (한계 절의 근거) · 런 불필요

**왜.** §4.1 의 세 숫자가 지금 재현 가능한 형태로 없다. 논문 한계 절이 이 계산에
의존하므로 감사 가능해야 한다.

**할 일.**
1. `experiments/mbe_param_model.py` — 논문 G.1 의 `N` + 모델 구조에서 MBE 뉴런
   파라미터 총수를 유도. **검증 앵커: 사이트 인벤토리 합이 339 여야 한다.**
2. 규약 세 개를 전부 출력(§4.1).
3. **논문 부록에서 "항등원은 학습 파라미터를 고정한다"(G.1)의 정확한 의미 확인.**
   "고정"이 사이트마다 같은 상수 1벌이면 저쪽 항등원 218개가 1개로 붕괴해 총 5,978 이
   되고 **우리가 2.1× 진다.** 그리고 그건 **저쪽 판 tying** 이므로 우리 tied 항등원이
   차별점이 아니라 동률이 된다. **이 축 전체의 전제다.** 런 불필요, 논문 읽기.

**완료 조건.** 스크립트가 세 규약 숫자를 뽑고, "고정" 문장의 해석이 문서화됨.
**E6 이후에 돌릴 것** — tying 이 우리 쪽 숫자를 바꾼다.

---

### E7 — 메모리 접근 포함 비용 모델 · 런 불필요

**왜.** §0 에서 MoE 프레임을 채택한 순간 **"뱅크 전환 비용은?"** 을 자초한다. 그리고
NLSpike 가 Table 3 에서 데이터 이동을 자기 강점으로 선점했다. 뱅크 전환은 지역성이
나쁜 쪽이라 **재서 질 수 있다.** E11 의 점유 분포가 이 모델의 입력이 된다(전환 빈도).

**템플릿.** MoE 문헌의 expert 로딩 오버헤드 계상 방식을 그대로 쓴다.

**완료 조건.** 숫자가 나온다. **져도 보고한다** — 그 경우 헤드라인을 정확도·스파이크로
좁히고(이미 그렇다) 한계 절에 적는다. 안 재고 넘어가는 것보다 낫다.

---

### E12 — 캘리브레이션 배치 분산 · 🟡 **1단계 완료 2026-08-10 / 2단계 열림**

> **결과** → `PASN_vault/60 - 연구일지/E12 - 캘리브레이션 배치 분산.md`,
> `results/e12_calib_var.json`(3 레코드). 로컬 CPU 빌드 3회 63분.
>
> ✅ **손잡이 신설** — 캘리브레이션이 train split 첫 8블록으로 **하드코딩**돼 있었다.
> `--calib-offset` 으로 창을 미끄러뜨린다(`src/` 무수정, 프리즈 중이므로).
> draw 3벌은 **pairwise overlap 0**.
> ✅ **무음성 증명(함정 9)** — offset 0 의 총 스파이크 **2,533,421,274** 가
> `timestep_sens_cost.json` 의 `t16-unif` 와 **마지막 자리까지 일치**. 기존 모든
> 레코드는 offset-0 레코드다.
> ✅ **불변**: 저장 **완전 동일**(53,888/13,472/339), 예산 `(N_j,T_j)` **19/19 뱅크
> 동일**, 스파이크 spread **0.0112%**, 에너지 **0.0017%**, op 별 전부 0.03% 안.
> ⚠️ **가변**: 캘리브레이션 range 는 엔드포인트의 **67–69% 가 움직인다** — 중앙값
> **3.2–4.1%**, 최대 **24.3%**. **뉴런들은 서로 다른 정의역 위에서 적합된다.**
> 📌 **왜 규칙이 안 움직이나** — `b = log₂(Δ_j/√ε)` 에서 3–4% 이동은 **0.05비트**라
> 이산 임계를 못 넘는다. **규칙이 잡음을 흡수한다** → "예산을 손튜닝이 아니라 푼다" 는
> 기여의 **안정성 근거**로 쓸 수 있다.
> 📌 **이동이 어텐션에 몰린다**(165/240 사이트, 최대도 `av_matmul`·`attn_softmax`) —
> 예산·오차·점유에 이어 **네 번째 측정이 같은 프리미티브를 짚는다.**
> 🔴 **2단계는 열려 있다 — 이건 ΔPPL 오차막대가 아니다.** 구조가 같아도 같은 구조 안의
> 적합 파라미터는 다르다(range 가 움직였으므로). **헤드라인 −0.19% 에 붙일 숫자는
> 아직 없다.** 다만 구조가 전혀 안 움직이므로 ΔPPL 이 크게 움직일 이유도 적어,
> **우선순위는 E13 뒤로 둘 만하다.**

### (원문) E12 설계 근거 · 🔴 **격상 2026-08-10 — 이제 방어 필수**

**왜.** `NEXT_EXPERIMENTS.md` §4 가 실제 분산원을 **체크포인트와 캘리브레이션 배치**로
못박았다. 체크포인트는 NLU 에서 특성화했다(SST-2 0.37pp). **캘리브레이션은 한 번도 안
쟀다.** 그런데 GPT-2 헤드라인이 **−0.19%** 이고 stride 만 바꿔도 부호가 바뀐다
(+0.058%). "무손실" 주장에 오차막대가 없으면 그 −0.19% 가 재현될 값인지 알 수 없다.

> ⚠️ **E0 이 이걸 nice-to-have 에서 필수로 옮겼다.** 이제 **같은 stride 에서 두 빌드가
> −0.140% / −0.187%** 를 낸다. 리뷰어의 첫 질문은 **"0.047 pp 는 빌드 차이인가
> 잡음인가"** 이고, 빌드는 결정론적이므로(seed 무의미) **남은 분산원은 캘리브레이션
> 하나뿐이다.** 그 답이 없으면 E0 의 결정 자체를 방어할 수 없다.

**⚠️ seed 스윕이 아니다.** 빌드는 결정론적이다(`seed=0`/`7` state dict 동일). 바꾸는
것은 **캘리브레이션에 쓰는 문서/배치**이지 난수가 아니다. 이걸 seed 스윕으로 오해해
1시간 45분을 이미 날린 적이 있다.

**설계 — 싼 것부터.** ① 캘리브레이션 셋 3벌로 **빌드만** 해서 사이트별 적합 오차와
바이트를 비교한다(로컬 CPU). 빌드 층위 분산이 작으면 거기서 닫는다. ② 크면 그때만
**1–2점 GPU 재평가.**

**완료 조건.** 헤드라인 문장에 붙일 분산 수치가 나오고, 마진 1.76pp 이 그 분산의 몇
배인지 말할 수 있다(NLU 에서 2.3× 를 근거로 쓴 것과 같은 형태).

---

### E4 — PASN-S on GPT-2 ★ 배선 이미 완료

**왜.** 함수 4종 단위에서 **전역 MBE 를 3축 전부에서 지배**한다
(`results/shared_banks_report.md`): gelu MSE 40,000× / 스파이크 1.4× / params 동률,
silu 29,000× / 2.5× / 동률, exp2 2.7× / 3.3× / **params 2.0× 적음**,
invsqrt 7.8× / 1.8× / **1.2× 적음**. 경계 점프도 자기 RMS 오차 대비 2–13× 작다.

**중요 — 이건 배선 프로젝트가 아니라 런이다.** `mbe_pasn_s` 는 이미
`_ROUTED_BACKENDS` 에 있고(`convert.py:512`), `build_mbe_pasn_s` 도, 러너 플래그도
전부 있다. **그런데 실행 기록이 0건이다** — `results/` 의 PASN 레코드 22개가 전부
`mbe_pasn`(flat) 이다.

**주의 — 예산 규칙이 그대로 넘어가지 않는다.** S 는 전역 `n_shared` 하나에 뱅크별
readout 이라 `N_j` 배분이 의미를 잃는다. S 의 knob 은 `n_shared` + `spike_budget` 이다.
`N_j` 규칙을 S 에 이식하려면 재유도가 필요하고, **그건 이 항목의 범위가 아니다.**

**flat 을 버리지 말 것.** 모든 함수에서 **flat 이 최소 스파이크점을 갖고**, S 의 어떤
설정도 등정확도에서 그 밑으로 못 간다. 논문 서술은 *"flat 은 저에너지 끝을, 공유
basis 는 그 위 전 영역의 정확도와 메모리를 갖는다."*

**완료 조건.** GPT-2-medium Stage 2 에서 `mbe_pasn_s` 한 점의 ΔPPL·스파이크·바이트.
flat 과 나란히 놓아 **어느 쪽도 지배당하지 않음**을 보인다.

---

### E8 — CV: ViT × ImageNet

**왜.** 논문은 CV/NLU/NLG 3종, 우리는 2종. C10 의 전제.

**배선은 거의 공짜.** `hf_convert.py` 가 모델 불문이고 ViT 가 같은
`eager_attention_forward` 시그니처·같은 두 레지스트리 디스패치를 쓰는 것이 확인됐다.
`ACT_TARGETS` 가 `GELUActivation → exact gelu` 로 매핑하는데 ViT 가 쓰는 게 그것이다.

**막는 것 — 유일한 블로커.** ImageNet val 5만장(~7GB) 라이선스·다운로드. **vast.ai 필요.**
**조달이 크리티컬 패스다** — 8월 안에 안 되면 ICML 사이클에 못 들어간다(§7 리스크 3).

**완료 조건.** ViT-B/16 top-1 conversion loss 를 논문 −0.44% 와 나란히.

---

### E9 — 더 큰 모델 1개

**왜.** 스케일 비판(우리 345M vs NLSpike 8B급 4종). 완화만 가능하고 봉쇄는 불가.
**8B 를 쫓지 말 것** — 예산으로 불가능하고 따라가는 순간 프레임이 저쪽 것이 된다.
**"깨지지 않는다"만 보이면 된다.** 덤으로 Table XI 의 교차 모델 단서(C3)도 완화된다.

**비용.** GPU. 기준점은 **gpt2-medium 1점 = 2.4 h**(13.3 h 가 아니다 — E0 비용 주석
참조). 파라미터 2× 면 대략 5 h 급이라 **E9 는 생각보다 싸다.** 그래도 **먼저 스케일링
추정부터.** 후보: GPT-2-large 또는 Pythia-1.4B 급.

---

### E10 — NLSpike 포지셔닝 · 런 불필요

**왜.** 동시기 경쟁 논문(arXiv:2605.20289). 문제 설정이 같고 프리미티브 분해도 거의
같지만, 저쪽은 LIF population + **LUT** 이고 파이프라인 **뒤에** 붙는 플러그인이다.

**최소한 반드시**: Related Work + 차별점 표. 저쪽이 손으로 정한 `H=5, K=64` 와 우리
**푼 예산 규칙**을 나란히 놓는 것만으로 기여가 선명해진다.

**여유 있으면**: 직교성 실험("우리 라우팅 위에 저쪽 LUT 연산자를 얹으면?"). **직교성을
보이면 경쟁 논문이 아니라 보완 논문이 된다** — 리뷰 정치상 유리하다.

---

## 3. 프리즈에서 **뺀** 것 — 의도적 결정

| 항목 | 왜 미루나 |
|---|---|
| **softmax→matmul 융합** | 가장 큰 남은 에너지 표적(어텐션 86.5%, `av_matmul` 이 최대 사이트 오차)이지만 **수치를 크게 바꿔 전면 재평가**를 부른다. 게다가 E1 이 **값어치가 운영점 의존**임을 보였다(T=4 에서 matmul 18.4%). **다음 사이클.** |
| **τ 뱅크 간 공유** | 바이트의 54–61% 라 레버는 크지만 메모리 축은 헤드라인이 아니다(§0). **E6 tying 과 더하지 말 것** — tied 뱅크는 이미 τ 를 공유한다. |
| **라우터 축퇴 지점의 예산 재탐색** | 규칙이 `(2,16)`=17.32 스파이크를 고르는데 `(3,8)`=8.76 이 **더 정확**(1.98×, 3 seed 동일). `rule_budget` 소폭 수정이지만 수치 변경. **E11 이 축퇴 사이트를 찾으면 같이 처리.** |
| **P0.5 잔여** | matched 토이 에너지(`waterfall.py` 재실행), Table XI 를 gpt2-medium 에서 재측정. 후자는 **E9 에 흡수 가능.** |

---

## 4. 계측 규약 — 어기면 숫자가 무의미해진다

### 4.1 파라미터 비교의 세 규약

MBE 뉴런 파라미터는 `N` 의 닫힌 함수다: **일반 6N+1, 극성분할(GELU/SiLU) 12N+1**
(스윕 데이터 N ∈ {1,2,4,6,8} 전부 확인). 사이트 수는 모델 구조에서 나온다.

⚠️ **아래 PASN 열은 전부 `--no-pasn-beta` 빌드의 12,488 로 계산된 stale 값이다**
(프리즈 빌드는 **13,472**). E5 가 다시 계산한다 — 표시된 비율을 그대로 인용하지 말 것.

| 규약 | MBE | PASN *(stale, 12,488 기준)* | 비율 *(stale)* |
|---|---|---|---|
| 우리 공유·타잉 적용 vs 논문 G.1 계산 | 16,611 | 12,488 → **13,472** | 1.33× → **~1.23×** 승 |
| **양쪽 다 공유 없음 (중립)** | 16,611 | 29,399 → 재계산 필요 | **0.57× 패**, 더 나빠짐 |
| 논문 "고정 항등원"을 1벌 공유로 읽으면 | 5,978 | 12,488 → **13,472** | 0.48× → **~0.44×** 패 |

**1.33× 를 그대로 쓰지 말 것.** 12,488 의 절감 대부분이 **cross-site 공유(2.354×)**
에서 오는데 이건 **라우팅과 무관하고 MBE 도 할 수 있다.** 우리한테만 주면 불공정하고,
이 프로젝트는 이미 한 번 데였다(P0.5 의 바이트 역전 = 공유 프로토타입 한쪽만 중복 청구).

**규약 세 개를 다 보고하는 것이 정직하고 오히려 강하다.**
⚠️ **PASN 쪽 12,488 은 프리즈 빌드에서 13,472 다**(E0 실측 확정, 08-10). 위 표 세 줄을
**전부 13,472 로 다시 계산해야 한다** — 중립 규약은 0.57× 에서 더 나빠지고, 1.33× 승도
**1.23×** 로 줄어든다. **E5 를 지금 돌릴 수 있다**(프리즈 선행조건 해소).

### 4.2 그 밖

* **등정확도에서만 비용을 비교한다.** 단일 운영점 비교는 정확도를 스파이크로 살 수 있다.
* **같은 박스는 필요조건이지 충분조건이 아니다.** 절대 스파이크는 환경 의존이고
  ratio 도 arm 마다 다르게 움직인다(`N_j` 분해가 두 박스에서 6.6% 차이). **바이트는
  두 박스에서 동일** — 저장이 단단한 축이다.
* **프리미티브 단위 변경을 op 의 나머지를 고정한 채 판정하지 말 것.** `1/x` 라우터가
  그 설계 때문에 ~1.0× 로 **두 번** 잘못 닫혔다. 질문은 "이 프리미티브의 스파이크가
  얼마냐"가 아니라 **"등정확도에서 op 이 얼마냐"** 다.
* **빌드를 바꾸는 손잡이는 전부 레코드에 적는다** (E0-1). 지금 `pasn_beta` 가 빠져 있어
  헤드라인 레코드의 빌드를 소급 판정할 수 없다.
* **결과 JSON 은 원격 런이 끝날 때마다 커밋한다.** 닫힌 vast.ai 박스가 P0.4 Block C 와
  Stage 2 원본 레코드를 통째로 가져간 적이 있다.

### 4.3 op 예산 표는 **운영점 없이 존재할 수 없다** (E0 신설 2026-08-10)

연구일지가 *"실모델 — 이쪽이 기준이다"* 로 못박아 둔 **어텐션 86.5% / 활성함수 3.3%**
는 `s2fix-rel-1e-2`, 즉 **규칙이 `T_j` 를 배분한 arm** 의 값이다. **논문과 비교하는
운영점(전역 `T=16`)에서는 구성이 다르다:**

| arm | matmul | softmax | LN | **활성함수** | 어텐션 계 |
|---|---:|---:|---:|---:|---:|
| per-bank `T_j` (`s2fix-rel-1e-2`) | 48.6% | 37.9% | 10.2% | **3.3%** | **86.5%** |
| **전역 `T=16` = 헤드라인·프리즈 빌드** | 48.3% | 33.5% | 10.0% | **8.1%** | **81.8%** |
| T=8 alloc (`e1-t8-alloc`) | 21.7% | 39.2% | 29.6% | 9.6% | 60.9% |

이유는 구조적이다 — 규칙은 활성함수 꼬리에 `T≈3` 을 주는데 전역 `T=16` 이 그걸 16 으로
올린다(`act_spikes_per_input` **1.52 → 3.82**). **둘 다 살아 있는 숫자이고, 어느 arm
인지 붙이지 않으면 둘 다 틀린 인용이 된다.**

**정정해야 하는 파생 문장 둘:**
* *"활성함수·LayerNorm 최적화의 상한은 전체 1.16×"* → **전역 `T=16` 에서는 1.22×**
* *"실험 1·2·4·6 은 예산의 13.5% 를 다투고 있었다"* → **전역 `T=16` 에서는 18.2%**

**방향은 안 바뀐다** — 어텐션이 여전히 최대 항목이고 융합이 최대 표적이다. 다만 §3 이
이미 적어둔 *"융합의 값어치가 운영점 의존"* 이 T 축뿐 아니라 **arm 축에서도** 참이다.

---

## 5. 하지 말 것

`NEXT_EXPERIMENTS.md` §4 전체가 유효하다. 특히:

* **seed 스윕 금지** — 빌드가 결정론적이다(`seed=0`/`7` state dict 동일). 1시간 45분을
  이미 날렸다. 실제 분산원은 **체크포인트**와 **캘리브레이션 배치**다(→ E12).
* **절대 정확도/perplexity 를 논문과 비교 금지.**
* **우리 MBE 재현을 헤드라인 기준선으로 쓰지 말 것.**
* **NLSpike 의 8B 실험을 따라 하지 말 것.**
* **한 운영점에서 잰 절감을 다른 점에서 인용하지 말 것.**
* **프리즈 전에 GPU 스윕을 시작하지 말 것.** (08-09 신설 — §2 도입부)
* **`−0.14%` · `49,952 B` · `12,488 p` 를 인용하지 말 것.** (08-10 신설) 프리즈 빌드는
  **−0.19% / 53,888 / 13,472** 다. 옛 값은 `--no-pasn-beta` 빌드의 것이고, 그 빌드의
  결과를 **인용해야 할 때는 반드시 그 이름을 붙인다**(예: stride 스윕, Stage 2 원본).
* **op 예산 비중을 arm 없이 인용하지 말 것.** (08-10 신설 — §4.3) "어텐션 86.5%" 는
  per-bank `T_j` arm, 헤드라인 빌드는 **81.8%** 다.
* **헤드라인 범위는 한 arm 에서 나와야 한다 — op 마다 최선 arm 을 고르지 말 것.**
  (08-10 신설 — E6 §9) `1.61–6.41×` 가 그 형태였다(진단용으로는 옳고, 실험 12 표는
  arm 을 적어 뒀다). 논문은 **`pasn_rule`** — 변환 경로가 실제로 짓는 것 — 하나로 쓴다
  → **10/10, `1.23×–5.39×`**. 세 arm 비교는 §3 워터폴로 따로 싣는다.

---

## 6. 채울 수 없는 칸 — 그것도 결론이다

* **SST-5 / Subj**: 공개 base fine-tune 이 없다. 유일한 `large` 후보
  (`ghatgetanuj/roberta-large_cls_subj`)는 93.35 로 논문 97.50 대비 4.15pp 뒤져
  conversion loss 해석이 불가능하다. 채우려면 **우리 ANN 을 직접 fine-tune** 해야 하고
  그건 다른 프로젝트다 — **조용히 메우지 말고 의도적으로 결정할 것.**
* **MR 은 판정 불가이고 그게 발견이다.** 같은 fine-tune 계열 업로드 둘이 −0.73% 와
  −0.10% 를 주고 논문의 −0.44% 가 그 사이에 있다. **범위를 보고한다.**

---

## 7. 일정

| 시기 | 할 일 | 축 |
|---|---|---|
| ~~8/9–8/12~~ | ✅ **완료** — E11 감사 · E11b 판정 · E6 배선 · E0 배선·귀속 | — |
| ~~8/12–8/13~~ | 🟡 **부분 완료** — E0 재평가(3.3 h) ✅ · E1 T=8(1.9 h) ✅ · **T=4/2 유실** | GPU |
| ~~8/10~~ | ✅ **완료** — E0 결정 문서화 · E6 후속(op 표 재산출) · **E12 1단계**(빌드 3회) | 로컬 |
| ~~8/10~~ | 🟡 **E1 T=4 완료(+2.14%), T=2 진행** — 게이트 ANN 21.7058 재현 | GPU |
| **8/11–8/14** | **E3 문헌 확인**(Table 4 의 `T` 대응이 기준선 문서에 없다 — 아래 리스크 7) → **E5** | 로컬 |
| 8/10–8/16 (병렬) | E3 문헌 확인 + `--dataset` 배선, E10 서술 | 로컬 |
| 8/20–8/28 | **E13 안 A**(stride 512, ~5 h GPU), 초록·그림(E11 의 MoE 그림) | 혼합 |
| **~8/30** | **NeurIPS 2026 워크숍 제출** — 현재 결과 + E0 + E1 + E10 | — |
| 9–10월 | E2, E3(런), E4, E8(ImageNet 조달이 되면) | 혼합 |
| 11월 | E7, E9, E10 정량, 융합(§3) 검토 | 혼합 |
| 12월 | 초고 + 내부 리뷰 | — |
| **1/29/2027** | **ICML 2027 제출** (주 타깃) | — |
| 5/16/2027 | (리젝 시) NeurIPS 2027 | — |

**ICLR 2027(9/26)은 권하지 않는다** — CV 미완 + 스케일 격차인 채로 A* 에 들어간다.

### 정직한 리스크

1. ~~**E0 이 헤드라인을 움직일 수 있다.**~~ ✅ **발생했고 처리됐다(08-10).** 밴드를
   **벗어났고**(0.187% vs 0.14%), 계획대로 **의도적 결정**을 해서 beta 빌드를 논문
   빌드로 확정했다. 헤드라인은 **−0.19%**, 마진은 1.71 → **1.76 pp** 로 오히려 커졌다.
   **남은 것은 이 결정이 만든 두 부채다** — C6 회귀(→ **E13**)와 오차막대 부재
   (→ **E12**, 이제 필수). **둘 다 알려진 부채이지 미지수가 아니다.**
2. **E11 이 축퇴 사이트를 더 찾을 수 있다.** 찾으면 프리즈가 밀린다. 그래도 리뷰어가
   찾는 것보다 낫고, 고치면 `1/x` 처럼 **이득**이 될 가능성이 높다(0.85×→5.39×).
3. **E2 가 실패할 수 있다.** 그 경우 §진단 절을 다시 쓰고 내부 ablation 을 뺀다(C9 삭제).
4. **E7 을 재서 질 수 있다.** 뱅크 전환은 지역성이 나쁜 쪽이다. 그때는 정직하게 보고하고
   헤드라인을 정확도·스파이크로 좁힌다(이미 그렇게 잡혀 있다).
5. **ImageNet 조달이 8월 안에 안 되면 E8 이 ICML 사이클에 못 들어간다.**
   조달→어댑터→런→분석이 최소 6–8주다. **8월 중 착수 여부를 결정할 것.**
6. **스케일 비판은 완전히 못 막는다.** E9 로 완화할 뿐. 기여의 축을 스케일이 아니라
   **"예산을 푼다"** 로 잡아야 방어가 성립한다.
7. **🔴 신설 08-10 — 논문 Table 4 의 `T` 대응을 우리가 기록해 두지 않았다.**
   `PASN_vault/50 - Reference/MBE 논문 published 설정과 수치.md` 에 수열
   `22.65 → 41072 → 11992 → 33.56 → 23.41` 만 있고 **어느 값이 어느 `T` 인지 없다.**
   핀이 박힌 건 **T=16 = 23.41 (+3.36%)** 하나뿐이다(Table 3 의 Wiki-103 행과 일치).
   나머지 셋은 단조 가정이 그럴듯하지만 **추정이고, 이 프로젝트는 추정으로 닫았다가
   두 번 틀린 전례가 있다**(`1/x`, 함정 15). **E1 곡선을 Table 4 옆에 놓기 전에
   논문을 열어 확인할 것 — 런 불필요, E3 와 같은 성격.**

---

관련: `NEXT_EXPERIMENTS.md`(현황·함정), `PASN_method.md`(방법 스펙, §14 열린 확장),
`PASN_vault/60 - 연구일지/00 - 연구일지 인덱스.md`(실험 기록 인덱스),
`PASN_vault/40 - Planning/논문 계획...md`(벤뉴 분석, NLSpike 조사),
`PASN_vault/50 - Reference/MBE 논문 published 설정과 수치.md`(기준선 수치).
