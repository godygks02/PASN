# MBE 뉴런 구현 노트 — 그대로 구현 · 발생한 문제 · 수정 사항

이 문서는 MBE 뉴런을 CPU 환경에서 구현·검증하는 과정에서 **논문을 그대로 옮긴
부분**, **구현 중 실제로 발생한 문제**, 그리고 **각 문제를 어떻게 진단하고
수정했는지**를 시간순으로 정리한 기록이다. 최종 수치 결과는
[`MBE_RESULTS.md`](MBE_RESULTS.md)에, 코드는 [`src/mbe/`](src/mbe/)에 있다.

관련 코드:
[`neuron.py`](src/mbe/neuron.py) ·
[`surrogate.py`](src/mbe/surrogate.py) ·
[`functions.py`](src/mbe/functions.py) ·
[`fit.py`](src/mbe/fit.py)

---

## 1. 논문 그대로 구현한 부분

논문(AAAI-26, "Training-Free ANN-to-SNN Conversion")의 MBE 뉴런 수식은 그대로
옮겼다. basis `n`, 시간 `t ∈ [0, T-1]`에 대해:

- **파라미터 갱신 (Eq.4)** — 지수 감쇠 커널
  `Para(τ_n, t) = α · exp(-t·Δt/τ_n)`,  `τ_n ∈ {τ_d, τ_r, τ_Vth}`
- **basis 동역학 (Eq.5-7)**
  - `u_n[0] = x`  (막전위 초기값 = 입력)
  - `s_n[t] = H(u_n[t] - Vth_n[t])`  (Heaviside 발화)
  - `u_n[t+1] = u_n[t] - s_n[t]·r_n[t]`  (발화 시 reset)
  - `o_n[t+1] = o_n[t] + s_n[t]·d_n[t]`  (강도 누적)
- **뉴런 출력 (Eq.8)** — `f̂(x) = Σ_n w_n · o_n[T]`
- **학습 파라미터 basis당 5개** (`τ_d, τ_r, τ_Vth, Δt, w`) → 총 `5N`개
  (Table VI). `α`는 하이퍼파라미터, 초기값 `d_n[0], r_n[0], Vth_n[0]`는
  `α, Δt, τ`로 결정 (Appendix E.1).
- **벡터화** — `M`개 샘플 × `N` basis를 텐서 연산으로, `T`(=16)만 루프.
- **decay 제거 ablation** — 커널을 시간 상수로 고정하는 `decay=False` 옵션.

초기 스모크 테스트는 통과했다: 출력 shape 정상, `5N`(=20) 파라미터, 역전파
gradient 흐름 정상, firing rate 측정 가능.

> 여기까지는 "수식만" 옮긴 상태다. 문제는 **이 뉴런으로 실제 함수를 피팅했을
> 때** 드러났다.

---

## 2. 구현 중 발생한 문제와 수정 (시간순)

논문의 검증 기준은 Table X (함수별 근사 MSE, N=8에서 ~5e-5)다. 아래는 이 수치에
도달하기까지 실제로 부딪힌 문제들이다.

### 문제 ①: 첫 피팅이 논문보다 1,000배 나쁨
- **증상**: 1/√x, 1/x, 2ˣ를 N=8로 피팅 → MSE `8.9e-2 / 2.7e-1 / 3.5e-2`.
  논문은 `4.9e-5 / 4.4e-5 / 5.3e-5`. firing rate가 0.08~0.16으로 매우 낮음
  (뉴런이 거의 발화하지 않음).
- **진단**: MBE/FS 뉴런의 임계값·reset 커널은 입력을 **이진 탐색(successive
  approximation, ADC)** 방식으로 분해한다. 이게 제대로 되려면 커널이 매 스텝
  절반씩 줄어야 한다 → `exp(-Δt/τ) = 1/2` → `τ = Δt/ln2 ≈ 1.44`. 처음의
  log-spaced 초기화는 이 지점을 완전히 벗어나 있었다.
- **수정**: `τ_r, τ_Vth`를 이진 탐색 지점(halving)으로 초기화.
- **결과**: 여전히 ~1e-1. 초기화만으로는 부족했다 → 더 깊은 문제 존재.

### 문제 ②: 도메인 최솟값에서 출력이 0으로 붕괴 (DC baseline 손실)
- **증상**: 2ˣ를 3000 epoch 학습하니 2.5e-3까지 내려갔으나, `x=0`에서
  `pred=0.44`인데 `target=1.0`. 오차가 도메인 경계에 집중.
- **진단**: 당시 막전위를 `u[0] = x - x_min`으로 시프트했는데, 이러면 가장 작은
  입력이 `u=0`이 되어 **어떤 임계값도 넘지 못해 영원히 발화 안 함 → 출력 0**.
  GELU처럼 `f(x_min)≈0`인 함수는 괜찮지만, 1/x·2ˣ처럼 `f(x_min)≠0`인 함수는
  baseline을 표현할 수 없다.
- **수정**: 양수 도메인은 시프트 제거(`x_min=0`), `alpha_v=hi/2`로 감쇠하는
  임계값이 가장 작은 입력도 잡도록 함.
- **결과**: ~1e-3 (개선됐지만 아직 논문보다 30배 높음).

### 문제 ③: multi-basis가 작동하지 않음 (basis 붕괴)
- **진단 (핵심)**: "고정 임계값에서 최적 강도(intensity)를 선형 최소제곱으로
  풀면 얼마까지 가능한가?"를 계산 → **단일 이진 코드의 한계는 ~1e-3**이고, 이는
  **논문 N=1 열과 정확히 일치**(1/√x 1.4e-3 등). 즉 N=1은 이미 맞다. 문제는
  N을 8로 늘려도 N=1과 똑같다는 것.
- **원인**: 모든 basis가 **같은** `alpha_v`를 공유 → 첫 임계값이 전부 같은
  레벨 → basis들이 서로 **중첩(nested)된 이진 코드**가 됨. 실제로 특징 행렬이
  **특이(singular)**했고, basis별 평균 스파이크 수가 `[14.6, 6.8, 3.9, ...]`로
  극도로 중복. LBFGS를 써도 1.6e-3에서 멈춤 → 최적화가 아니라 **표현력** 문제.
- **수정 (가장 중요한 수정)**: **basis마다 서로 다른 `alpha_v,n`** (선두 임계값)을
  부여 → 서로 다른 레벨에 독립적인 다해상도(multi-resolution) 코드 형성. 선형
  최소제곱 상한이 즉시 `~1e-5`로 떨어짐을 확인.
- **결과**: 실제 학습에서 **invsqrt 1.3e-5, 2ˣ 1.3e-4, inv 3.2e-4** — 논문
  수준 도달 (약 100배 개선). 여기에 **학습 가능한 출력 DC bias**도 추가해
  `f(x_min)≠0` baseline을 깔끔히 처리.

### 문제 ④: GELU 실패 (넓은 도메인 + 0 근처 비단조 굴곡)
- **증상**: GELU(−120,10)는 N=8에서도 MSE `0.15~0.4`. LBFGS가 130 스케일에서
  overflow.
- **진단**:
  1. 도메인 (−120,10)의 **92%가 GELU≈0인 평탄 영역**. 실제 곡률은 정규화 범위
     상위 ~11%(x≈0 부근)에 몰려 있는데, 균일 샘플링·균일 임계값은 엉뚱한
     평탄부만 잘게 나눔.
  2. 잔차를 뜯어보니 오차가 **x∈[−1, 0.5]의 날카로운 굴곡**(음의 딥 후 상승)에
     100% 집중. 선형 꼬리(x>2)는 잘 맞음.
- **수정**:
  - **입력 정규화** `u=(x−x_min)/x_scale ∈ [0,1]` → 130 스케일 제거, 수치
    안정화(LBFGS overflow 해결), 양수/부호 도메인 처리 통일.
  - **`alpha_v` 학습 가능화** + **곡률 기반 배치**(|f′|의 분위수에 선두 임계값을
    배치 = Algorithm 1 step 3 "collect intervals R" 정신).
  - LBFGS 라운드가 나빠지면 롤백하는 안전장치.
- **1차 결과**: 곡률 배치로 GELU 임계값이 활성 영역으로 이동(0.15→0.07)했으나
  **여전히 ~7e-2**. 원인은 **부호(signed)/극성(polarity) 메커니즘의 부재** —
  0 근처의 음의 딥은 음의 `w`와 bias만으로는 표현이 부족.

- **최종 해결 (수정 ④-A, ④-B)** — 두 가지로 완전히 해결:
  - **④-A. Signed(극성 분할) 뉴런** ([`src/mbe/signed.py`](src/mbe/signed.py)):
    입력을 x=0에서 `relu(x)`(양수부)와 `relu(−x)`(음수부 크기)로 쪼개 각각 별도
    MBE 뱅크로 인코딩하고 하나의 readout으로 합침. GELU(−120,10)의 표현 상한이
    **0.5 → 2.9e-5**로 급락. 이는 [`PASN_method.md`](PASN_method.md) §6이 요구한
    signed-magnitude 분해의 구현이며, 모든 PASN 뱅크에 동일 적용된다.
  - **④-B. Readout 닫힌 형태 해**(`fit.solve_readout`): `f̂ = features(x)·w + bias`
    는 나머지 파라미터가 고정되면 `(w, bias)`에 대해 **선형** → SVD 최소제곱으로
    정확해가 존재. 이를 shape 파라미터 gradient step 사이에 교대로 풀어주니,
    surrogate 훈련이 표현 상한까지 도달. **모든 함수 MSE가 약 1자릿수 개선**.
  - **결과**: GELU N=8 = **1.7e-4** (논문 1.0e-4), 단조 함수는 N=8에서
    **5e-6 수준**으로 논문(~5e-5)을 오히려 능가. (§4 표 참조)

---

## 3. 최종 구현 형태 (수정 반영)

논문 대비 **추가/변경된 설계 결정**(모두 논문이 명시하지 않은 부분):

| 항목 | 논문 | 본 구현 | 이유 |
|---|---|---|---|
| Surrogate gradient | 미명시 | ATan(기본) + LBFGS 단계에서 sharpening | 역전파에 필요 |
| 입력 스케일 | (암묵) | `[0,1]` 정규화 (캘리브레이션) | 수치 안정성, 도메인 통일 |
| 선두 임계값 `α_v` | 단일 하이퍼파라미터 | **basis별**, 곡률 기반 배치, 학습 가능 | 다해상도 표현(핵심) |
| 출력 bias | 없음 (LN의 β만) | 학습 가능한 DC offset 1개 | `f(x_min)≠0` baseline |
| Readout 최적화 | lr0.01/200ep/exp0.99 | Adam + LBFGS + **닫힌 형태 `(w,bias)` 해** | 표현 상한 도달(핵심) |
| 부호 처리 | 미명시 | **극성 분할 signed 뉴런**(GELU/SiLU) | 비단조 음의 딥 표현 |
| 파라미터 수 | `5N` | 기본 `6N+1`, 옵션으로 `5N` 복원 | `α_v` 학습화로 +N |

> `learn_alpha_v=False, use_bias=False`로 설정하면 **논문의 정확히 `5N`
> 파라미터 형태**로 되돌아간다 (유닛 테스트로 검증).

---

## 4. 검증 결과 요약

Table X (N=8, 각 셀 = 본구현 / 논문):

| 함수 | 본 구현 N=8 | 논문 N=8 | decay 제거(N=8*) |
|---|---|---|---|
| GELU (signed) | **1.7e-4** | 1.0e-4 | 1.4e-2 |
| 1/√x | **5.0e-6** | 4.9e-5 | 2.2e-4 |
| 1/x | **4.6e-6** | 4.4e-5 | 8.2e-4 |
| 2ˣ | **6.5e-5** | 5.3e-5 | 1.2e-3 |

- **4개 함수 모두 논문 수준 재현** — N=8에서 논문과 동급이거나 능가(단조 함수는
  ~10× 우수). GELU도 signed 뉴런으로 논문 수준 도달(과거 ~1e-1 → 1.7e-4).
- **N=1이 논문 N=1 floor와 일치**(1/√x 1.3e-3 vs 1.4e-3) → N 스케일링도 재현.
- **decay ablation** 모든 함수 1~2 자릿수 악화 → 지수 감쇠 필수 주장 재현.
- **Table VII (SiLU)** 세 구간 모두 논문 이상: [−8,−2] 5.8e-5, [−2,5] 7.6e-4,
  [2,12] 6.0e-5.

상세 표·그래프는 [`MBE_RESULTS.md`](MBE_RESULTS.md), 유닛 테스트는
[`tests/test_mbe_neuron.py`](tests/test_mbe_neuron.py)(8/8 통과).

---

## 5. 남은 이슈 (다음 단계)

1. **논문의 exact 5N·고정 α 형태 정밀 검증** — 본 구현은 MBE *동역학*은 충실하나
   논문 미명시 메커니즘(per-basis α, 닫힌형 readout, signed)을 보강함. 논문에 쓰인
   그대로의 뉴런이 그 수치를 내는지는 별도 확인 필요.
2. **PASN 라우터·뱅크 실제 구현** — 현재는 R=1 등가성만 테스트.
3. **FP-mult / Softmax / LayerNorm 조립**(Phase 3), 이후 GPU(vast.ai)에서
   Transformer 변환·다운스트림 벤치마크(Phase 4~5). 이것이 논문의 헤드라인
   결과이며 아직 미착수.
