# 메소드 설명 — MBE‑PASN 과 PASN

이 문서는 우리 연구의 두 뉴런, **MBE‑PASN**과 **PASN**을 설명한다. 둘은 같은 뼈대
(입력 범위를 로컬 구간으로 나눠 각 구간을 전담 인코더가 처리)를 공유하지만, **구간 안
인코더가 다르다**. 그 차이가 두 뉴런의 정체성과 연구 포지셔닝을 가른다.

---

## 0. 한 문단 요약

- 공통 아이디어: 입력의 **부동소수점(FP) 지수 비트**를 읽어 입력을 2의 거듭제곱 구간
  (**binade**)으로 라우팅하고, 각 구간을 **전용 로컬 뱅크**가 근사한다. 입력마다 뱅크
  **하나만** 활성화된다(하드 라우팅 = MoE식).
- **MBE‑PASN**: 각 뱅크가 **MBE 뉴런**(지수감쇠 multi‑basis, 경사하강으로 학습). →
  기존 **MBE 뉴런의 개선안**.
- **PASN**: 각 뱅크가 **독자 인코더**(고정 SAR 스파이크 코드 + 학습 선형 readout). →
  MBE와 동역학·학습·구조가 다른 **독자 뉴런**.

---

## 1. 배경 — 왜 로컬 뱅크인가

training‑free ANN→SNN 변환은 신경망의 각 비선형(GELU, Softmax의 exp, LayerNorm의
1/√x 등)을 **스파이크로 근사**하는 문제다. 기준선인 **MBE 뉴런**은 스칼라 함수 `f(x)`를
`N`개의 지수감쇠 시간 basis의 스파이크 합으로 근사한다 — 단, **입력 도메인 전체를 하나의
전역 뱅크**로 처리한다.

이 전역 방식의 한계를 우리는 실측으로 확인했다: GELU처럼 **0 근처에 곡률이 몰린** 함수를
넓은 범위에서 하나의 뉴런으로 근사하면, 해상도 예산을 0 근처와 넓은 꼬리에 동시에 배분하지
못해 **0 근처에서 오차 바닥(~0.07)** 이 생긴다. basis를 늘려도, 실분포로 학습해도 안 줄었다.
이 오차가 깊은 Transformer(GPT‑2 24층)에 누적되면 perplexity가 폭발한다.

→ **해법: 입력 범위를 여러 구간으로 나눠, 각 구간을 좁게(=거의 선형으로) 전담 근사한다.**

---

## 2. 공통 뼈대 — Prefix 라우팅 + binade 로컬 뱅크

두 뉴런이 공유하는 부분이다.

### 2.1 FP 지수 = 공짜 coarse code
부동소수점 수 `x`는 `x = ±2^e·(1+ρ)` 로 쓸 수 있다 (`e`=지수, `ρ∈[0,1)`=가수/잔차).
- **부호 비트**와 **지수 비트 `e`** 를 그냥 읽으면 → `x`가 어느 **binade**
  `|x|∈[2^e, 2^{e+1})` 에 속하는지 즉시 안다. 스파이크 0, 파라미터 0, 정확.
- 지수가 **크기 척도 `2^e`를 공짜로** 알려주므로, 뱅크는 **구간 안 위치 `ρ`만** 근사하면 된다.

### 2.2 0 근처가 자동으로 촘촘하다
binade는 로그 간격이다: `…[1,2), [½,1), [¼,½), [⅛,¼)…`. **0에 가까울수록 구간이
지수적으로 좁아진다.** 즉 곡률이 몰린 0 근처에 **자동으로 전용 뱅크가 많이** 배정된다 —
전역 뉴런이 원리적으로 못 하던 것.

### 2.3 하드 라우팅(MoE) + 단일부호 뱅크
- 입력마다 **뱅크 하나만** 활성(하드 top‑1 라우팅). 나머지는 계산 안 함 → 에너지 절약.
- 라우터는 **학습 파라미터가 없다**(비트 읽기). MoE의 게이팅을 "결정적 오라클"로 대체.
- 부호가 최상위 비트이므로 각 크기 뱅크는 **단일부호 구간**만 본다. 0을 걸치는 뱅크는
  near‑zero 뱅크 하나뿐.

```
공통 흐름:
  x ──(부호·지수 비트 읽기)──▶ 뱅크 인덱스 (binade)
                              └▶ 구간 안 잔차 ρ ──▶ [뱅크 인코더] ──▶ f(x)
```

**차이는 오직 `[뱅크 인코더]` 안이다.**

---

## 3. MBE‑PASN — MBE 뉴런의 개선안

코드: `src/mbe/mbe_pasn.py` (`MBEPASNNeuron`, `build_mbe_pasn`).

- 각 뱅크 = **완전한 MBE 뉴런**. MBE의 지수감쇠 동역학
  (`d[t], r[t], Vth[t] = α·exp(−t·Δt/τ)`)을 **그대로** 쓴다.
- 뱅크는 자기 구간의 함수 `g(ρ)=f(±2^e(1+ρ))` 를 근사하도록 **경사하강 + 닫힌형
  readout**으로 학습된다(원래 MBE와 동일한 학습 절차).
- **적응 예산**: 평탄한 꼬리 구간은 basis `N=1`, 곡률 큰 near‑zero 구간은 큰 `N` — 구간별로
  다른 예산.
- **R=1 성질**: 구간을 하나로 두면(전역 단일 뱅크) 원래 **MBE 뉴런과 완전히 동일**하다.
  즉 MBE‑PASN은 MBE의 순수한 확장(superset)이다.

> 포지셔닝: **"MBE 뉴런 + prefix 로컬 뱅크"** 로, MBE의 직접적 개선안. 단, MBE 동역학을
> 그대로 쓰므로 "MBE와 별개"라고 주장하긴 어렵다.

---

## 4. PASN — 독자 뉴런 (Prefix‑Adaptive Spiking Neuron)

코드: `src/mbe/pasn.py` (`PASNNeuron`, `build_pasn`).

MBE 동역학을 **버리고**, 뱅크 안을 두 개의 원리적 부품으로 새로 짰다.

### 4.1 인코딩 — 고정 SAR(연속근사) 스파이크 코드
구간 안 잔차 `ρ∈[0,1)` 를 **연속근사(successive approximation)** 로 스파이크화한다.
저울에 반씩 다는 것과 같다:

```
u = ρ
lvl = 1/2
for t in 1..T:                # T = 비트 수 = 스파이크 예산
    s = 1 if u ≥ lvl else 0   # ← 스파이크(예/아니오)
    u = u − s·lvl             # ← 발화하면 그만큼 빼기(리셋)
    lvl = lvl / 2             # 다음 자리(절반)
```

- 각 스텝의 예/아니오가 **이진 스파이크**. **임계·발화·리셋**을 갖춘 정식 스파이킹
  뉴런이다(SNN 문헌의 **FS few‑spike 뉴런** 계열 — MBE 논문의 비교 대상이기도 하다).
- **고정**이다. 학습하지 않는다. 대부분 입력은 몇 개의 스파이크만 낸다(에너지↓).

### 4.2 디코딩 — 뱅크별 선형(저차) readout만 학습
스파이크 코드에서 복원한 `ρ̂` 로 저차 다항 특징 `[1, ρ̂, ρ̂², …]` 를 만들고,
**뱅크마다 작은 선형 readout `w`** 로 `f(x)` 를 낸다.

- `w` 는 그 구간의 샘플로 **최소제곱(closed‑form)** 으로 즉시 푼다. **경사하강·surrogate
  gradient가 전혀 없다.** → **강한 의미의 training‑free**, 빌드가 밀리초.
- 구간이 좁으므로 **선형(order 1)만으로도** 정확하고, order 2면 오차가 한 자릿수 더 내려간다
  (스파이크 수는 그대로, 저장 계수만 몇 개 추가).

### 4.3 왜 MBE와 "다른 뉴런"인가

| | MBE (뱅크) | PASN (뱅크) |
|---|---|---|
| 동역학 | 학습된 지수감쇠 커널 | **고정 SAR(이진 할빙)** |
| 학습 대상 | 커널 τ들 + readout (경사하강) | **readout만 (최소제곱)** |
| multi‑basis | N개 지수 basis | SAR 비트/저차 특징 |
| 성격 | 학습 동역학 | ADC식 코드 + 회귀 |

동역학·학습·구조가 전부 다르다. 그래서 PASN은 "MBE 개선"이 아니라 **multi‑basis와 로컬
뱅크 아이디어를 쓴 독자 뉴런**으로 제시한다.

---

## 5. 두 뉴런 한눈 비교

| 항목 | MBE (기준선) | **MBE‑PASN** | **PASN** |
|---|---|---|---|
| 구조 | 전역 단일 뱅크 | prefix 로컬 뱅크 | prefix 로컬 뱅크 |
| 뱅크 인코더 | — | MBE 뉴런 | 고정 SAR + 선형 readout |
| 학습 | 경사하강+readout | 경사하강+readout(뱅크별) | **readout만(최소제곱)** |
| 라우터 파라미터 | 없음 | 없음(결정적) | 없음(결정적) |
| 입력당 활성 | 전체 | 뱅크 1개 | 뱅크 1개 |
| MBE와의 관계 | 본체 | 개선안(R=1=MBE) | **독자(무관)** |
| 코드 | `neuron.py` | `mbe_pasn.py` | `pasn.py` |

---

## 6. 실측 요약 (함수근사, 정확도·에너지·메모리)

`results/neuron_comparison_report.md` 의 3자 비교 결과 요약 (best config, MSE / 스파이크 /
파라미터):

| 함수 | MBE(전역) | MBE‑PASN | **PASN** |
|---|---|---|---|
| gelu | 3.8e‑4 / 46 / 49 | 1.7e‑5 / 18 / 181 | **1.0e‑4 / 3.0 / 63** |
| exp2 | 1.6e‑5 / 64 / 49 | 1.2e‑5 / 13 / 77 | **3.9e‑6 / 3.0 / 21** |
| invsqrt | 8.0e‑6 / 77 / 49 | 2.3e‑5 / 7 / 64 | **6.0e‑6 / 3.0 / 24** |

- **에너지**: PASN은 모든 함수에서 **~3 스파이크/입력** (MBE 대비 7~25×, MBE‑PASN 대비
  2~6× 적음).
- **메모리**: PASN 14~63 파라미터(readout만) — MBE‑PASN(51~181)보다 훨씬 가벼움.
- **정확도**: 매끄러운 함수는 3축 모두 지배. GELU/SiLU는 MBE‑PASN이 MSE만 약간 낮지만
  스파이크·메모리를 몇 배 더 쓴다.

---

## 7. 연구 포지셔닝

- **MBE‑PASN** = MBE 뉴런의 **개선안**. MBE 논문의 published 표를 기준선으로 비교.
- **PASN** = multi‑basis + 로컬 뱅크 아이디어를 쓴 **독자 뉴런**. MBE 재현이 완벽하지 않아도
  독립적으로 주장 가능하며, 마찬가지로 MBE 논문 표를 벤치마크로 삼는다.
- 두 뉴런을 **함께** 제시해, "개선안"과 "독자안"의 서로 다른 강점(MBE‑PASN=정확도,
  PASN=에너지·메모리·단순성)을 비교하는 것이 이 연구의 구도다.

관련 문서: `PASN_method.md`(정식 스펙), `results/neuron_comparison_report.md`(비교 결과),
vault `00 - Overview/PASN Method.md`.
